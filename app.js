(() => {
  'use strict';

  const roles = {
    admission: { label: 'Admission Nurse', initials: 'AN', limit: 1000, canApprove: false, canOverride: false, vendorAdmin: false, reports: 'team' },
    charge: { label: 'Case Manager', initials: 'CM', limit: 1500, canApprove: true, canOverride: false, vendorAdmin: false, reports: 'team' },
    don: { label: 'Director of Nursing', initials: 'DN', limit: 3000, canApprove: true, canOverride: true, vendorAdmin: true, reports: 'clinical' },
    executive: { label: 'Executive', initials: 'EX', limit: Infinity, canApprove: true, canOverride: true, vendorAdmin: true, reports: 'executive' },
    clinician: { label: 'Clinician', initials: 'MD', limit: 0, canApprove: true, canOverride: false, vendorAdmin: false, reports: 'clinical' }
  };

  const defaultOrders = [
    { id:'BRX-10482', patient:'Jordan M.', vendor:'ABC Home Medical', orderedBy:'A. Smith · Admission Nurse', status:'Dispatched', exact:'En Route · 18 min away', risk:68, riskLabel:'High', eta:'3:00–3:30 PM', items:['Hospital Bed E0260','Oxygen Concentrator E1390','Specialty Mattress E0277'], cost:1720, benchmark:1485, weather:true, verified:true, delivered:false, acknowledged:false, pickup:'Not requested', location:'I-215 · 7.8 miles away', messages:[{me:false,text:'Both the bed and concentrator are on the truck.',time:'2:08 PM'},{me:true,text:'Thank you. Please confirm the specialty mattress.',time:'2:10 PM'}], timeline:['Order created · 12:42 PM','Standing orders verified · 12:48 PM','DON override approved · 1:02 PM','Vendor verified · 1:08 PM','Dispatched · 1:37 PM','En route · 2:04 PM'], escalations:[] },
    { id:'BRX-10479', patient:'Elena R.', vendor:'Mountain DME', orderedBy:'C. Lee · Case Manager', status:'Delivered', exact:'Proof submitted · awaiting acknowledgment', risk:22, riskLabel:'Low', eta:'Delivered 11:46 AM', items:['Wheelchair E1161','Over-bed Table E0274'], cost:685, benchmark:711, weather:false, verified:false, delivered:true, acknowledged:false, pickup:'Not requested', location:'Delivery complete', messages:[{me:false,text:'Delivery completed. Setup reviewed with family.',time:'11:48 AM'}], timeline:['Order created · 8:12 AM','Vendor accepted · 8:19 AM','Dispatched · 9:34 AM','Delivered · 11:46 AM','Proof and condition attestation received · 11:48 AM'], escalations:[] },
    { id:'BRX-10471', patient:'Arthur K.', vendor:'ABC Home Medical', orderedBy:'J. Perez · Admission Nurse', status:'Pending Pickup', exact:'Pickup scheduled · Tomorrow 10 AM–12 PM', risk:31, riskLabel:'Moderate', eta:'Delivered Aug 12', pickupEta:'Tomorrow · 10 AM–12 PM', items:['Hospital Bed E0260','Patient Lift E0630'], cost:1240, benchmark:1180, weather:false, verified:false, delivered:true, acknowledged:true, pickup:'Scheduled', location:'Patient residence', messages:[], archivedMessages:true, timeline:['Delivered · Aug 12','Delivery acknowledged · Aug 12','Messages auto-archived · Aug 12','Pickup requested · Aug 14','Pickup scheduled · Aug 16'], escalations:[] },
    { id:'BRX-10462', patient:'Mae L.', vendor:'Front Range Medical', orderedBy:'A. Smith · Admission Nurse', status:'Complete', exact:'Picked up · condition confirmed', risk:12, riskLabel:'Low', eta:'Delivered Aug 8', pickupEta:'Completed Aug 13', items:['Oxygen Concentrator E1390'], cost:422, benchmark:465, weather:false, verified:false, delivered:true, acknowledged:true, pickup:'Complete', location:'Closed', messages:[], archivedMessages:true, timeline:['Delivered · Aug 8','Acknowledged · Aug 8','Pickup requested · Aug 12','Picked up · Aug 13','Complete · Aug 13'], escalations:[] },
    { id:'BRX-10458', patient:'Samuel T.', vendor:'Summit Respiratory', orderedBy:'C. Lee · Case Manager', status:'Dispatched', exact:'Awaiting driver assignment', risk:79, riskLabel:'Critical', eta:'Expected 5:00–6:00 PM', items:['BiPAP E0470','Suction Pump E0600'], cost:2180, benchmark:1760, weather:true, verified:false, delivered:false, acknowledged:false, pickup:'Not requested', location:'Vendor warehouse', messages:[{me:false,text:'Dispatch is checking the weather route.',time:'1:32 PM'}], timeline:['Order created · 12:10 PM','Executive approval requested · 12:22 PM','Approved · 12:31 PM','Vendor accepted · 12:39 PM','Awaiting driver · 1:20 PM'], escalations:[{from:'Case Manager',to:'Director of Nursing',reason:'Post-submission item change: added Suction Pump',time:'12:45 PM',status:'Escalated to Clinician'},{from:'Director of Nursing',to:'Clinician',reason:'Clinical review required for respiratory bundle change',time:'12:52 PM',status:'Pending'}] },
    { id:'BRX-10495', patient:'Rosa V.', vendor:'Mountain DME', orderedBy:'A. Smith · Admission Nurse', status:'Dispatched', exact:'On schedule · no issues', risk:15, riskLabel:'Low', eta:'4:30–5:00 PM', items:['Wheelchair E1161'], cost:310, benchmark:320, weather:false, verified:false, delivered:false, acknowledged:false, pickup:'Not requested', location:'En route · 3.2 miles away', messages:[], timeline:['Order created · 1:15 PM','Standing orders verified · 1:18 PM','Vendor accepted · 1:22 PM','Dispatched · 2:45 PM','En route · 3:10 PM'], escalations:[] },
    { id:'BRX-10497', patient:'Henry W.', vendor:'ABC Home Medical', orderedBy:'C. Lee · Case Manager', status:'Dispatched', exact:'Dispatched · vendor confirmed', risk:42, riskLabel:'Moderate', eta:'5:30–6:00 PM', items:['Hospital Bed E0260','Oxygen Concentrator E1390'], cost:970, benchmark:940, weather:false, verified:true, delivered:false, acknowledged:false, pickup:'Not requested', location:'Vendor loading', messages:[{me:false,text:'Both items loaded. Departing shortly.',time:'3:22 PM'}], timeline:['Order created · 11:30 AM','Standing orders verified · 11:34 AM','Vendor verified · 11:48 AM','Dispatched · 3:15 PM'], escalations:[] },
    { id:'BRX-10499', patient:'Diana C.', vendor:'Front Range Medical', orderedBy:'J. Perez · Admission Nurse', status:'Delivered', exact:'Delivered · acknowledgment pending', risk:18, riskLabel:'Low', eta:'Delivered 2:10 PM', items:['Patient Lift E0630'], cost:820, benchmark:790, weather:false, verified:false, delivered:true, acknowledged:false, pickup:'Not requested', location:'Delivery complete', messages:[{me:false,text:'Lift installed and tested with family present.',time:'2:12 PM'}], timeline:['Order created · 9:45 AM','Vendor accepted · 9:52 AM','Dispatched · 11:30 AM','Delivered · 2:10 PM','Proof submitted · 2:12 PM'], escalations:[] },
    { id:'BRX-10501', patient:'Frank B.', vendor:'Summit Respiratory', orderedBy:'C. Lee · Case Manager', status:'Dispatched', exact:'Modification escalated · awaiting clinician', risk:55, riskLabel:'High', eta:'Expected 6:00–7:00 PM', items:['Oxygen Concentrator E1390','Suction Pump E0600'], cost:820, benchmark:780, weather:false, verified:true, delivered:false, acknowledged:false, pickup:'Not requested', location:'Vendor warehouse', messages:[{me:true,text:'Please confirm the suction pump addition.',time:'2:55 PM'}], timeline:['Order created · 1:50 PM','Standing orders verified · 1:55 PM','Vendor verified · 2:05 PM','Dispatched · 2:30 PM','Order modified · 2:50 PM','Escalation: Case Manager → DON · 2:52 PM','Escalation: DON → Clinician · 2:58 PM'], escalations:[{from:'Case Manager',to:'Director of Nursing',reason:'Post-submission addition of Suction Pump E0600',time:'2:52 PM',status:'Reviewed'},{from:'Director of Nursing',to:'Clinician',reason:'Clinical necessity review for added respiratory equipment',time:'2:58 PM',status:'Pending'}] },
    { id:'BRX-10503', patient:'Gloria H.', vendor:'ABC Home Medical', orderedBy:'A. Smith · Admission Nurse', status:'Dispatched', exact:'Normal delivery in progress', risk:20, riskLabel:'Low', eta:'4:00–4:30 PM', items:['Hospital Bed E0260'], cost:540, benchmark:560, weather:false, verified:false, delivered:false, acknowledged:false, pickup:'Not requested', location:'En route · 5.1 miles', messages:[], timeline:['Order created · 10:20 AM','Standing orders verified · 10:24 AM','Vendor accepted · 10:30 AM','Dispatched · 2:00 PM','En route · 3:30 PM'], escalations:[] }
  ];

  const defaultVendors = [
    { id:'abc', name:'ABC Home Medical', area:'Salt Lake & Davis Counties', categories:['Beds / support surfaces','Oxygen / respiratory','Mobility'], active:12, onTime:94, contacts:'Sarah Martinez · Dispatch · (801) 555-0142', email:'dispatch@abchomemedical.demo', address:'4830 West Medical Way, Salt Lake City, UT', hours:'24/7 urgent fulfillment', events:[{type:'Positive',category:'Exceptional service',severity:'Low',note:'Accepted urgent bed order in 18 minutes.',date:'Aug 12'}] },
    { id:'mountain', name:'Mountain DME', area:'Salt Lake County', categories:['Beds / support surfaces','Mobility','Bathroom / safety'], active:7, onTime:97, contacts:'Riley Chen · Operations · (801) 555-0188', email:'dispatch@mountaindme.demo', address:'2100 South State Street, Salt Lake City, UT', hours:'6 AM–10 PM; after-hours on call', events:[{type:'Positive',category:'Equipment condition',severity:'Low',note:'Consistently complete condition photos.',date:'Aug 10'}] },
    { id:'front', name:'Front Range Medical', area:'Salt Lake, Utah & Tooele Counties', categories:['Mobility','Patient transfer','Oxygen / respiratory'], active:3, onTime:88, contacts:'Devon Brooks · Escalations · (801) 555-0117', email:'dispatch@frontrangemedical.demo', address:'9950 South Commerce Drive, Sandy, UT', hours:'7 AM–9 PM', events:[{type:'Negative',category:'Delivery timeliness',severity:'Moderate',note:'Two late lift deliveries during July.',date:'Jul 29'}] },
    { id:'summit', name:'Summit Respiratory', area:'Wasatch Front', categories:['Oxygen / respiratory','Suction','Ventilation'], active:5, onTime:91, contacts:'Morgan Hale · Respiratory Dispatch · (801) 555-0171', email:'dispatch@summitrespiratory.demo', address:'7500 South 300 West, Midvale, UT', hours:'24/7 respiratory support', events:[] }
  ];

  const defaultApprovals = [
    { id:'APR-208', patient:'Samuel T.', order:'BRX-10458', amount:2180, reason:'$420 above contracted respiratory bundle; urgent BiPAP required.', level:'Executive', requester:'C. Lee', status:'Approved' },
    { id:'APR-211', patient:'Nora P.', order:'Draft 33', amount:1260, reason:'Specialty mattress outside Admission Nurse authority.', level:'Case Manager', requester:'A. Smith', status:'Pending' },
    { id:'APR-214', patient:'Evan D.', order:'Draft 35', amount:3475, reason:'Nonstandard bariatric package, 7-day episode override requested.', level:'Director of Nursing', requester:'C. Lee', status:'Pending' }
  ];

  const defaultState = {
    screen:'landing', role:'admission', view:'home', orderFilter:'Active', messageFilter:'Active', selectedOrder:null, selectedVendor:null,
    orders:defaultOrders, vendors:defaultVendors, approvals:defaultApprovals,
    notifications:[
      {id:1,text:'Order BRX-10479 has been delivered.',detail:'Proof and condition attestation are ready.',order:'BRX-10479',unread:true},
      {id:2,text:'Vendor verification required.',detail:'Severe weather affects BRX-10458.',order:'BRX-10458',unread:true},
      {id:3,text:'Cost approval requested.',detail:'Nora P. · Draft 33 · $1,260',view:'home',unread:true}
    ],
    bundles:[
      {id:'comfort',name:'Comfort & Mobility Admission',items:['Hospital Bed E0260','Oxygen Concentrator E1390','Wheelchair E1161'],note:'Common admission package; each item still requires standing-order verification.'},
      {id:'respiratory',name:'Respiratory Support',items:['Oxygen Concentrator E1390','Suction Pump E0600'],note:'Urgent respiratory package with vendor-availability confirmation.'},
      {id:'transfer',name:'Safe Transfer',items:['Hospital Bed E0260','Patient Lift E0630'],note:'Bed and transfer support for high-assistance admissions.'}
    ],
    handoff:{shift:'Night → Day',owner:'A. Smith',note:'Confirm BRX-10458 vendor capacity before 3 PM. BRX-10479 proof is ready for acknowledgment.',acknowledged:false,updated:'6:42 AM'},
    preferences:{inApp:true,push:true,criticalAlways:true,quietStart:'21:00',quietEnd:'06:00',phiPreview:false,cost:true,risk:true,delivery:true,pickup:true},
    costPolicy:{admission:1000,charge:1500,don:3000,donDays:7,executiveDays:30},
    finance:{
      weekly:{label:'Weekly',revenue:164250,dmeCost:18740},
      monthly:{label:'Monthly',revenue:711400,dmeCost:82460},
      quarterly:{label:'Quarterly',revenue:2098700,dmeCost:246900},
      annual:{label:'Annual',revenue:8156000,dmeCost:971300}
    },
    psfCases:[
      {id:'PSF-240',order:'BRX-10458',vendor:'Summit Respiratory',failure:'Missed dispatch commitment',status:'Open',revenueAtRisk:1320,recovered:0,opened:'Today · 1:24 PM'},
      {id:'PSF-237',order:'BRX-10482',vendor:'ABC Home Medical',failure:'Specialty mattress omitted from initial load',status:'Under Review',revenueAtRisk:940,recovered:0,opened:'Today · 11:18 AM'},
      {id:'PSF-231',order:'BRX-10462',vendor:'Front Range Medical',failure:'Late pickup extended rental exposure',status:'Recovered',revenueAtRisk:760,recovered:760,opened:'Aug 13'},
      {id:'PSF-226',order:'BRX-10479',vendor:'Mountain DME',failure:'Incomplete proof delayed billing',status:'Recovered',revenueAtRisk:1080,recovered:1080,opened:'Aug 12'}
    ],
    draft:{ehr:false,patient:'',address:'',items:['Hospital Bed E0260','Oxygen Concentrator E1390'],categories:['Beds / support surfaces','Oxygen / respiratory'],standing:false,timeline:'4',rentalDays:21,severeEvent:true,disruptionType:'Winter Storm Warning',vendor:'abc',verified:false,vendorVerificationFailed:false,vendorVerifyNote:'',vendorVerifiedBy:'',vendorVerifiedAt:'',vendorVerificationResponses:null,notes:'',savedLocal:false,duplicateResolution:'',duplicateNote:'',duplicateResolvedBy:'',duplicateResolvedAt:'',duplicatePendingDisposition:'',duplicatePendingNote:''},
    preauthorized:['Mobility','Beds / support surfaces','Oxygen / respiratory','Bathroom / safety'], offline:false, lastEnrollment:null,
    pendingAdmits:[
      {id:'PA-001',name:'Margaret S.',mrn:'MRN-90102',diagnosis:'COPD exacerbation',admitDate:'Aug 15',location:'2847 Pine Valley Dr, West Jordan, UT',physician:'Dr. R. Okafor',notes:'Requires respiratory support and mobility aid. Standing orders on file.'},
      {id:'PA-002',name:'William T.',mrn:'MRN-90115',diagnosis:'Post-hip replacement',admitDate:'Aug 15',location:'1120 Maple Creek Ln, Murray, UT',physician:'Dr. L. Chen',notes:'Discharge from IMC. Needs bed and patient lift for home recovery.'},
      {id:'PA-003',name:'Catherine J.',mrn:'MRN-90128',diagnosis:'End-stage heart failure',admitDate:'Aug 16',location:'5540 South 900 East, Salt Lake City, UT',physician:'Dr. A. Patel',notes:'Hospice admit. Comfort care equipment package anticipated.'},
      {id:'PA-004',name:'Robert N.',mrn:'MRN-90131',diagnosis:'ALS progression',admitDate:'Aug 16',location:'3301 East Wasatch Blvd, Holladay, UT',physician:'Dr. M. Torres',notes:'Respiratory and suction equipment. Complex care coordination required.'},
      {id:'PA-005',name:'Betty F.',mrn:'MRN-90144',diagnosis:'Post-stroke rehabilitation',admitDate:'Aug 16',location:'880 North Main St, Bountiful, UT',physician:'Dr. K. Yamamoto',notes:'Wheelchair and bathroom safety. Low-complexity order expected.'}
    ]
  };

  let state = loadState();
  let ui = { notifications:false, profile:false, globalSearch:'', orderSearch:'', orderVendor:'All', orderOwner:'All', orderStatus:'All', vendorTab:'profile', vendorEvent:false, actionModal:null, modalId:null, newStage:1, messageOrder:'BRX-10482' };
  const $app = document.getElementById('app');

  function clone(v){ return JSON.parse(JSON.stringify(v)); }
  function loadState(){
    try { const saved = localStorage.getItem('betterrx-provider-demo'); return saved ? {...clone(defaultState),...JSON.parse(saved)} : clone(defaultState); }
    catch { return clone(defaultState); }
  }
  function save(){ localStorage.setItem('betterrx-provider-demo', JSON.stringify(state)); }
  function money(v){ return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v); }
  function esc(v){ return String(v ?? '').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])); }
  function toast(message){ const el=document.getElementById('toast'); el.textContent=message; el.classList.add('show'); clearTimeout(toast.timer); toast.timer=setTimeout(()=>el.classList.remove('show'),3500); }
  function copyText(value){
    const fallback=()=>{const area=document.createElement('textarea');area.value=value;area.style.position='fixed';area.style.opacity='0';document.body.appendChild(area);area.select();try{document.execCommand('copy');}finally{area.remove();}};
    if(navigator.clipboard?.writeText)navigator.clipboard.writeText(value).catch(fallback);else fallback();
  }
  function role(){ const r=roles[state.role]; return {...r,limit:state.role==='executive'?Infinity:(state.costPolicy?.[state.role]??r.limit)}; }
  function permission(action){
    if(state.role==='clinician'){
      if(action==='message') return true;
      if(action==='vendor') return false;
      if(action==='archive') return false;
      if(action==='exact') return true;
      return true;
    }
    if(action==='message') return ['don','executive'].includes(state.role);
    if(action==='vendor') return role().vendorAdmin;
    if(action==='archive') return state.role==='executive';
    if(action==='exact') return ['don','executive'].includes(state.role);
    return true;
  }
  function riskClass(n){ return n>=50?'red':n>=25?'amber':'green'; }
  function statusClass(s){ return s==='Complete'?'green':s==='Delivered'?'blue':s==='Pending Pickup'?'amber':'coral'; }
  function deliveryEta(o){ return o.status==='Dispatched' ? (o.eta||'Vendor update pending') : ''; }
  function pickupEta(o){
    if(!o.pickup||o.pickup==='Not requested') return '';
    if(o.pickupEta) return o.pickupEta;
    const match=String(o.exact||'').match(/Pickup (?:requested|scheduled) · (.+)$/i);
    return match?.[1]||(o.pickup==='Complete'?'Completed':'Vendor confirmation pending');
  }
  function contributionMetrics(period){
    const source=state.finance?.[period]||{label:period,revenue:0,dmeCost:0},contribution=source.revenue-source.dmeCost;
    return {...source,contribution,marginPct:source.revenue?contribution/source.revenue*100:0};
  }
  function vendorBenchmarkTrends(){
    const totals={};
    state.orders.forEach(o=>{const row=totals[o.vendor]||(totals[o.vendor]={vendor:o.vendor,cost:0,benchmark:0,orders:0});row.cost+=Number(o.cost)||0;row.benchmark+=Number(o.benchmark)||0;row.orders+=1;});
    return Object.values(totals).map(row=>({...row,variance:row.cost-row.benchmark,variancePct:row.benchmark?(row.cost-row.benchmark)/row.benchmark*100:0})).filter(row=>row.variance>0).sort((a,b)=>b.variancePct-a.variancePct);
  }
  function leadershipDashboard(){
    const periods=['weekly','monthly','quarterly','annual'].map(contributionMetrics),recovered=state.psfCases.filter(x=>x.status==='Recovered'),recoveredRevenue=recovered.reduce((sum,x)=>sum+(Number(x.recovered)||0),0),openExposure=state.psfCases.filter(x=>x.status!=='Recovered').reduce((sum,x)=>sum+(Number(x.revenueAtRisk)||0),0),trends=vendorBenchmarkTrends();
    return `<section class="leadership-dashboard"><div class="leadership-heading"><div><span class="eyebrow">DON & Executive dashboard</span><h2>Financial and service-recovery performance</h2><p>Preventable failures, vendor benchmark variance and contribution margin use the same operational record.</p></div>${pill('Leadership access','blue')}</div><section class="grid metrics leadership-metrics"><div class="metric"><span>PSF recoveries</span><strong>${recovered.length}</strong><small>Preventable failures recovered</small></div><div class="metric"><span>Revenue recovered</span><strong>${money(recoveredRevenue)}</strong><small>Closed PSF recovery value</small></div><div class="metric"><span>Open revenue exposure</span><strong>${money(openExposure)}</strong><small>${state.psfCases.length-recovered.length} PSFs need action</small></div><div class="metric"><span>Vendors above benchmark</span><strong>${trends.length}</strong><small>Current order portfolio</small></div></section><div class="grid two leadership-grid"><section class="card"><div class="card-head"><div><h2>Revenue, DME cost & contribution margin</h2><p class="muted">Contribution margin = revenue − direct DME cost.</p></div></div><div class="finance-table"><div class="finance-row finance-head"><span>Period</span><span>Revenue</span><span>DME cost</span><span>Contribution margin</span></div>${periods.map(p=>`<div class="finance-row"><strong>${p.label}</strong><span>${money(p.revenue)}</span><span>${money(p.dmeCost)}</span><span><strong>${money(p.contribution)}</strong><small>${p.marginPct.toFixed(1)}%</small></span></div>`).join('')}</div><p class="tiny muted">Prototype calculation treats DME as the direct variable cost shown here. Other variable costs can be added before using this as a final operating margin.</p></section><section class="card"><div class="card-head"><div><h2>Vendors trending above benchmark</h2><p class="muted">Aggregated actual DME cost versus CMS/local benchmark.</p></div>${pill(`${trends.length} above`,'amber')}</div><div class="list">${trends.map(v=>`<div class="row"><div class="row-main"><strong>${esc(v.vendor)}</strong><small>${v.orders} orders · ${money(v.cost)} actual vs ${money(v.benchmark)} benchmark</small></div><div class="row-meta">${pill(`+${v.variancePct.toFixed(1)}%`,'red')}<small>${money(v.variance)} above</small></div></div>`).join('')||'<div class="empty">No vendors are currently above benchmark.</div>'}</div></section></div><section class="card psf-card"><div class="card-head"><div><h2>Preventable Service Failures (PSF)</h2><p class="muted">Track recovery quantity, revenue at risk and revenue recovered.</p></div><div class="actions">${pill(`${recovered.length} recovered`,'green')}<button class="btn small" data-action="new-psf">+ Record PSF</button></div></div><div class="list">${state.psfCases.map(f=>`<div class="row ${f.status==='Open'?'high':''}"><div class="row-main"><strong>${esc(f.id)} · ${esc(f.vendor)}</strong><small>${esc(f.order)} · ${esc(f.failure)}</small><small>Opened ${esc(f.opened)}</small></div><div class="row-meta">${pill(f.status,f.status==='Recovered'?'green':f.status==='Open'?'red':'amber')}<small>${f.status==='Recovered'?`${money(f.recovered)} recovered`:`${money(f.revenueAtRisk)} at risk`}</small>${f.status!=='Recovered'?`<button class="btn small" style="margin-top:7px" data-recover-psf="${f.id}">Record recovery</button>`:''}</div></div>`).join('')}</div></section></section>`;
  }
  function addNotification(text,detail,order){ state.notifications.unshift({id:Date.now(),text,detail,order,unread:true}); }
  function titleBlock(kicker,title,body,actions=''){ return `<div class="page-head"><div><span class="eyebrow">${kicker}</span><h1>${title}</h1><p>${body}</p></div>${actions?`<div class="actions">${actions}</div>`:''}</div>`; }
  function pill(text,kind=''){ return `<span class="pill ${kind}">${esc(text)}</span>`; }

  const nav = [
    ['home','⌂','Home'],['new','＋','New Order'],['existing','▤','Existing Orders'],['past','◷','Past Orders'],['vendors','◎','Vendor Management'],['reports','↗','Reports'],['messages','✉','Messages']
  ];

  function render(){
    if(state.screen==='landing') return renderLanding();
    if(state.screen==='provider-login') return renderLogin();
    if(state.screen==='vendor-login') return renderVendorLogin();
    if(state.screen==='vendor-portal') return renderVendorPortal();
    if(state.screen==='join') return renderJoin();
    if(state.screen==='enrollment-complete') return renderEnrollmentComplete();
    renderPortal();
  }

  function renderLanding(){
    $app.innerHTML=`<main class="landing"><div class="landing-wrap">
      <div class="brand"><span class="brand-mark">Rx</span><div><strong>DME Direct</strong><small>Equipment Ordering & Visibility</small></div></div>
      <section class="hero"><span class="eyebrow">DME visibility, without the noise</span><h1>The right equipment.<br><span>Right when it matters.</span></h1><p>A shared, simple ordering and delivery workspace for hospice care teams and trusted DME partners.</p></section>
      <section class="portal-grid">
        <button class="portal-tile" data-action="provider-login"><i>✚</i><h2>Care Providers</h2><p>Order equipment, track deliveries, manage exceptions and coordinate pickups.</p></button>
        <button class="portal-tile" data-action="vendor-login"><i>▰</i><h2>DME Vendors</h2><p>Manage assigned orders, status updates, delivery proof and equipment returns.</p></button>
      </section>
      <div class="join-card"><button data-action="join">DME Vendor? Join our network! →</button><p class="tiny muted">Secure, scoped enrollment link · no patient or order access</p></div>
    </div></main>`;
  }

  function renderLogin(){
    $app.innerHTML=`<main class="landing"><div class="landing-wrap"><button class="btn" data-action="landing">← Back</button><div class="login-box">
      <span class="eyebrow">Provider access</span><h1>Choose a demo role</h1><p class="muted">The workspace changes to match each role’s responsibilities and authority.</p>
      <div class="role-list">${Object.entries(roles).map(([key,r])=>`<button class="role-option ${state.role===key?'selected':''}" data-role="${key}"><strong>${r.label}</strong><small>${key==='admission'?'Admission drafts and routine orders':key==='charge'?'Case coordination, team exceptions and first-level approvals':key==='don'?'Clinical oversight, vendor quality and 7-day overrides':key==='executive'?'Enterprise cost, network performance and 30-day overrides':'Clinical review, escalated orders and messaging'}</small></button>`).join('')}</div>
      <button class="btn primary" style="width:100%" data-action="enter-provider">Continue as ${role().label}</button><p class="tiny muted" style="text-align:center">Prototype access uses synthetic data only.</p>
    </div></div></main>`;
  }

  function renderVendorLogin(){
    $app.innerHTML=`<main class="landing"><div class="landing-wrap"><button class="btn" data-action="landing">← Back</button><div class="login-box">
      <span class="eyebrow">Existing vendor</span><h1>DME Vendor sign in</h1><p class="muted">Vendor operations remain separate from provider workflows.</p>
      <div class="field"><label for="vendorEmail">Email</label><input id="vendorEmail" type="email" value="dispatch@abchomemedical.demo" autocomplete="username"></div><div class="field" style="margin-top:12px"><label for="vendorPassword">Password</label><input id="vendorPassword" type="password" value="prototype" autocomplete="current-password"></div>
      <button class="btn primary" style="width:100%;margin-top:18px" data-action="vendor-demo">Sign in to vendor workspace</button><p class="tiny muted" style="text-align:center">Prototype credentials are prefilled for ABC Home Medical.</p>
    </div></div></main>`;
  }

  function renderVendorPortal(){
    const assigned=state.orders.filter(o=>o.vendor==='ABC Home Medical'&&o.status!=='Complete'),high=assigned.filter(o=>o.risk>=50),dispatched=assigned.filter(o=>o.status==='Dispatched'),pickupRequests=assigned.filter(o=>o.pickup&&o.pickup!=='Not requested'&&o.pickup!=='Complete');
    const inventory=[{serial:'SN-4821',hcpcs:'E0260',desc:'Hospital Bed',status:'Deployed',patient:'Jordan M.',daysOut:4},{serial:'SN-4822',hcpcs:'E0260',desc:'Hospital Bed',status:'Available',patient:'—',daysOut:0},{serial:'SN-3901',hcpcs:'E1390',desc:'Oxygen Concentrator',status:'Deployed',patient:'Arthur K.',daysOut:12},{serial:'SN-3902',hcpcs:'E1390',desc:'Oxygen Concentrator',status:'Maintenance',patient:'—',daysOut:0},{serial:'SN-2210',hcpcs:'E0277',desc:'Specialty Mattress',status:'Overdue',patient:'Henry W.',daysOut:34},{serial:'SN-5501',hcpcs:'E1161',desc:'Wheelchair',status:'Available',patient:'—',daysOut:0},{serial:'SN-5502',hcpcs:'E1161',desc:'Wheelchair',status:'Deployed',patient:'Gloria H.',daysOut:7},{serial:'SN-6601',hcpcs:'E0630',desc:'Patient Lift',status:'Available',patient:'—',daysOut:0}];
    const invStatusClass=s=>s==='Available'?'green':s==='Deployed'?'blue':s==='Overdue'?'red':'amber';
    const highRisk=assigned.filter(o=>o.risk>=50);
    $app.innerHTML=`<main class="vendor-preview"><header class="vendor-preview-header"><div class="brand"><span class="brand-mark">Rx</span><div><strong>DME Direct</strong><small>Vendor workspace</small></div></div><div class="actions"><span class="pill green">ABC Home Medical</span><button class="btn small" data-action="vendor-logout">Sign out</button></div></header><div class="vendor-preview-body">${titleBlock('Vendor operations','Operational Workspace','Fleet status, assigned orders, delivery proof, pickup management and SLA performance for ABC Home Medical.')}
      <section class="grid metrics"><div class="metric"><span>Total Fleet Size</span><strong>28</strong><small>Serialized units</small></div><div class="metric"><span>Available Units</span><strong>14</strong><small>Ready for dispatch</small></div><div class="metric"><span>On Route</span><strong>3</strong><small>Active deliveries</small></div><div class="metric"><span>Overdue for Pickup</span><strong>2</strong><small>Past scheduled return</small></div><div class="metric"><span>Current Load %</span><strong>68%</strong><small>Fleet utilization</small></div></section>
      ${highRisk.length?`<section class="card" style="border-left:4px solid var(--red)"><div class="card-head"><div><h2>At-Risk Orders</h2><p class="muted">Orders with risk ≥ 50 require priority action and explanation.</p></div>${pill(highRisk.length+' at risk','red')}</div><div class="list">${highRisk.map(o=>{const reasons=[];if(Number(o.risk)>=60)reasons.push('Tight timeline');if(o.weather)reasons.push('Severe weather advisory');reasons.push('High current load (68%)');if(o.items.length>1)reasons.push('Multi-item order complexity');return '<div class="row high"><div class="row-main"><strong>'+esc(o.id)+' · '+esc(o.patient)+'</strong><small>'+esc(o.items.join(' · '))+'</small><small><strong>Why at risk:</strong> '+esc(reasons.join(' + '))+'</small></div><div class="row-meta">'+pill('Risk '+o.risk,'red')+' '+pill(o.status,statusClass(o.status))+'</div></div>';}).join('')}</div></section>`:''}
      <section class="card"><div class="card-head"><div><h2>Equipment Inventory</h2><p class="muted">Serialized equipment tracking · ABC Home Medical fleet.</p></div></div><div class="list">${inventory.map(i=>`<div class="row"><div class="row-main"><strong>${esc(i.serial)} · ${esc(i.desc)}</strong><small>HCPCS ${esc(i.hcpcs)}${i.patient!=='—'?' · Patient: '+esc(i.patient):''} ${i.daysOut?' · '+i.daysOut+' days out':''}</small></div><div class="row-meta">${pill(i.status,invStatusClass(i.status))}</div></div>`).join('')}</div></section>
      <section class="card"><div class="card-head"><div><h2>Dispatched Orders & Delivery Proof</h2><p class="muted">Submit proof of delivery for dispatched orders.</p></div></div><div class="list">${dispatched.map(o=>'<div class="row '+(o.risk>=50?'high':'')+'"><div class="row-main"><strong>'+esc(o.id)+' · '+esc(o.patient)+'</strong><small>'+esc(o.items.join(' · '))+'</small><small><strong>Delivery ETA:</strong> '+esc(deliveryEta(o)||o.eta)+'</small></div><div class="row-meta">'+pill(o.vendorProofSubmitted?'Proof Submitted':o.status,o.vendorProofSubmitted?'green':statusClass(o.status))+(o.vendorSplit?' '+pill('Split Delivery','amber'):'')+(!o.vendorProofSubmitted?'<button class="btn small" style="margin-top:6px" data-action="vendor-proof" data-vendor-order="'+o.id+'">Submit Proof of Delivery</button>':'')+'<button class="btn small" style="margin-top:6px" data-action="vendor-split" data-vendor-order="'+o.id+'">Split Fulfillment</button></div></div>').join('')||'<div class="empty">No dispatched orders.</div>'}</div></section>
      <section class="card"><div class="card-head"><div><h2>Pickup Management</h2><p class="muted">Respond to provider pickup requests within SLA window.</p></div></div><div class="list">${pickupRequests.length?pickupRequests.map(o=>'<div class="row"><div class="row-main"><strong>'+esc(o.id)+' · '+esc(o.patient)+'</strong><small>'+esc(o.items.join(' · '))+'</small><small><strong>Pickup status:</strong> '+esc(o.pickup)+' · '+esc(pickupEta(o)||'Pending')+'</small><small class="muted" style="color:var(--red)">⏱ Response required within 4 hours</small></div><div class="row-meta">'+pill(o.pickup,'amber')+(o.pickup==='Requested'?'<button class="btn small" style="margin-top:6px" data-action="vendor-accept-pickup" data-vendor-order="'+o.id+'">Accept Pickup</button><button class="btn small" style="margin-top:6px" data-action="vendor-schedule-pickup" data-vendor-order="'+o.id+'">Schedule Pickup</button>':'')+'</div></div>').join(''):'<div class="empty">No pickup requests pending.</div>'}</div></section>
      <section class="card"><div class="card-head"><div><h2>SLA Performance Scorecard</h2><p class="muted">Contract performance metrics · current period.</p></div></div><div class="summary-strip"><div><span>On-Time Delivery Rate</span><strong>94%</strong></div><div><span>Pickup Response Time</span><strong>avg 2.1 hrs</strong></div><div><span>Order Completion Rate</span><strong>98%</strong></div><div><span>Contract Threshold</span><strong>90%</strong></div></div><div style="margin-top:12px"><div style="display:flex;align-items:center;gap:8px;margin-bottom:6px"><span style="width:140px;font-size:13px">On-Time Delivery</span><div style="flex:1;height:8px;background:#e8e8e8;border-radius:4px"><div style="width:94%;height:100%;background:var(--green);border-radius:4px"></div></div><small>94%</small></div><div style="display:flex;align-items:center;gap:8px;margin-bottom:6px"><span style="width:140px;font-size:13px">Completion Rate</span><div style="flex:1;height:8px;background:#e8e8e8;border-radius:4px"><div style="width:98%;height:100%;background:var(--green);border-radius:4px"></div></div><small>98%</small></div><div style="display:flex;align-items:center;gap:8px"><span style="width:140px;font-size:13px">Contract Threshold</span><div style="flex:1;height:8px;background:#e8e8e8;border-radius:4px"><div style="width:90%;height:100%;background:var(--amber);border-radius:4px"></div></div><small>90%</small></div></div></section>
      <div class="notice"><strong>Vendor preview active</strong><p>This sign-in demonstrates scoped vendor access. Provider approvals, ranking rules and other vendors remain unavailable.</p></div></div></main>`;
  }

  function renderJoin(){
    const token='enroll_vnd_7H2K9Q_demo';
    const equipment=['Beds / support surfaces','Oxygen / respiratory','Mobility','Patient transfer','Bathroom / safety','Ventilation','Suction','Enteral / nutrition'];
    $app.innerHTML=`<main class="landing enrollment-landing"><div class="landing-wrap enrollment-wrap"><button class="btn" data-action="landing">← Back</button><div class="login-box enrollment-box">
      <span class="eyebrow">Token verified · Enrollment only</span><h1>Join the DME Direct network</h1><p class="muted">This scoped link expires in 48 hours and does not provide access to patients, orders or provider data.</p>
      <div class="success" style="margin:16px 0"><strong>✓ Secure enrollment link active</strong><div class="tiny">${token}</div></div>
      <section class="enrollment-section"><div class="card-head"><div><h2>Company and dispatch contact</h2><p class="muted">Used for qualification, order routing and urgent availability confirmation.</p></div></div><div class="form-grid"><div class="field full"><label for="enrollCompany">Company name</label><input id="enrollCompany" value="Mile High Medical"></div><div class="field"><label for="enrollContact">Primary contact</label><input id="enrollContact" value="Taylor Reed"></div><div class="field"><label for="enrollPhone">Contact phone number</label><input id="enrollPhone" type="tel" value="(801) 555-0199" placeholder="(000) 000-0000"></div><div class="field"><label for="enrollEmail">Dispatch email address</label><input id="enrollEmail" type="email" value="dispatch@milehighmedical.demo" placeholder="dispatch@company.com"></div><div class="field"><label for="enrollServiceArea">Service area</label><input id="enrollServiceArea" value="Salt Lake County"></div></div></section>
      <section class="enrollment-section"><div class="card-head"><div><h2>Equipment availability</h2><p class="muted">Select every category currently fulfilled, or upload a catalog for direct review.</p></div></div><div class="check-list enrollment-equipment">${equipment.map((item,index)=>`<label class="check"><input type="checkbox" data-enrollment-category value="${item}" ${index<3?'checked':''}><span>${item}</span></label>`).join('')}</div><div class="catalog-upload"><label for="enrollCatalog"><strong>Upload equipment catalog</strong><span>CSV, Excel or PDF · replaces manual category selection when complete</span></label><input id="enrollCatalog" type="file" accept=".csv,.xlsx,.xls,.pdf"><small id="catalogFileName" class="muted">No catalog selected</small></div></section>
      <section class="enrollment-section"><div class="card-head"><div><h2>Dispatch coverage</h2><p class="muted">Enter actual staffed dispatch hours, not warehouse or customer-service hours.</p></div></div><div class="schedule-grid"><div class="schedule-row"><div><strong>Monday–Friday</strong><small>Standard weekday dispatch</small></div><div class="schedule-hours"><label>Opens<input id="enrollWeekdayStart" type="time" value="06:00"></label><label>Closes<input id="enrollWeekdayEnd" type="time" value="22:00"></label></div></div><div class="schedule-row"><div><strong>Weekends</strong><small>Saturday and Sunday dispatch</small></div><div class="schedule-choice"><label>Dispatches weekends<select id="enrollWeekendDispatch"><option value="yes">Yes</option><option value="no">No</option></select></label><label>Opens<input id="enrollWeekendStart" type="time" value="08:00"></label><label>Closes<input id="enrollWeekendEnd" type="time" value="18:00"></label></div></div><div class="schedule-row"><div><strong>Holidays</strong><small>Observed and federal holiday coverage</small></div><div class="schedule-choice"><label>Holiday coverage<select id="enrollHolidayDispatch"><option value="full">Full dispatch</option><option value="emergency">Emergency only</option><option value="none">No dispatch</option></select></label><label>Opens<input id="enrollHolidayStart" type="time" value="08:00"></label><label>Closes<input id="enrollHolidayEnd" type="time" value="16:00"></label></div></div></div></section>
      <div class="notice ranking-notice"><strong>How dispatch coverage affects matching</strong><p>Weekday, weekend and holiday availability is evaluated only when an order’s requested delivery window overlaps that condition. Equipment fit remains required, and historical item/timeline performance still contributes to the hidden deterministic ranking.</p></div>
      <button class="btn primary" style="width:100%;margin-top:18px" data-action="submit-enrollment">Submit network profile</button>
    </div></div></main>`;
  }

  function renderEnrollmentComplete(){
    const e=state.lastEnrollment;if(!e){state.screen='join';return renderJoin();}
    $app.innerHTML=`<main class="landing"><div class="landing-wrap"><div class="login-box enrollment-complete"><span class="eyebrow">Enrollment received</span><h1>${esc(e.company)} is ready for qualification</h1><p class="muted">The network team can now review equipment coverage and dispatch availability.</p><div class="success" style="margin:18px 0"><strong>✓ Contact and coverage profile saved</strong><span class="tiny">${esc(e.contact)} · ${esc(e.phone)} · ${esc(e.email)}</span></div><div class="summary-strip"><div><span>Equipment source</span><strong>${e.catalog?esc(e.catalog):`${e.categories.length} selected categories`}</strong></div><div><span>Weekday dispatch</span><strong>${esc(e.weekday)}</strong></div><div><span>Weekend dispatch</span><strong>${esc(e.weekend)}</strong></div><div><span>Holiday dispatch</span><strong>${esc(e.holiday)}</strong></div></div><div class="notice"><strong>Ranking inputs recorded</strong><p>These hours will affect matching only for orders whose requested delivery window falls during the applicable weekday, weekend or holiday period.</p></div><button class="btn primary" style="width:100%;margin-top:16px" data-action="landing">Return to landing page</button></div></div></main>`;
  }

  function renderPortal(){
    const unread=state.notifications.filter(n=>n.unread).length;
    const searchResults=ui.globalSearch.trim()?globalResults(ui.globalSearch):[];
    $app.innerHTML=`<div class="shell">
      <header class="topbar"><div class="brand"><span class="brand-mark">Rx</span><div><strong>DME Direct</strong><small>Provider workspace</small></div></div>
        <div class="global-search"><input id="globalSearch" placeholder="Search patient, order, vendor, clinician or HCPCS" value="${esc(ui.globalSearch)}" aria-label="Global search">${searchResults.length?`<div class="search-results">${searchResults.map(r=>`<button data-result-type="${r.type}" data-result-id="${r.id}"><strong>${esc(r.title)}</strong><small class="muted">${esc(r.detail)}</small></button>`).join('')}</div>`:''}</div>
        <div class="top-actions"><button class="icon-btn" data-action="toggle-offline" title="Connectivity">${state.offline?'◌':'●'}</button><button class="icon-btn" data-action="notifications">♢${unread?`<span class="badge">${unread}</span>`:''}</button><button class="profile-btn" data-action="profile"><span class="avatar">${role().initials}</span><div><strong>${role().label}</strong><small>Mountain View Hospice</small></div></button></div>
        ${ui.notifications?notificationPanel():''}${ui.profile?profileMenu():''}
      </header>
      <div class="layout"><aside class="rail"><nav>${nav.map(([id,icon,label])=>`<button class="${state.view===id?'active':''}" data-view="${id}"><span class="nav-icon">${icon}</span><span>${label}</span></button>`).join('')}</nav><div class="rail-footer"><strong>${state.offline?'◌ Offline · drafts local':'● Secure connection'}</strong><span>${state.offline?'Submission paused':'Encrypted session · Auto-lock on'}</span></div></aside>
      <main class="workspace">${viewMarkup()}</main></div></div>${state.selectedOrder?orderDrawer():''}${state.selectedVendor?vendorDrawer():''}${ui.vendorEvent?qualityModal():''}${ui.actionModal?actionModal():''}`;
  }

  function notificationPanel(){
    return `<div class="notification-panel"><div class="card-head"><h3>Notifications</h3><button class="btn small" data-action="read-all">Mark read</button></div>${state.notifications.slice(0,6).map(n=>`<button class="note-item ${n.unread?'unread':''}" data-notification="${n.id}"><strong>${esc(n.text)}</strong><small>${esc(n.detail)}</small></button>`).join('')}</div>`;
  }
  function profileMenu(){
    return `<div class="profile-menu"><strong>${role().label}</strong><p class="tiny muted">Mountain View Hospice</p><button class="btn" style="width:100%;margin-bottom:7px" data-view="settings">Notification & security settings</button><button class="btn" style="width:100%;margin-bottom:7px" data-action="provider-login">Switch role</button><button class="btn" style="width:100%;margin-bottom:7px" data-action="reset">Reset demo</button><button class="btn" style="width:100%" data-action="landing">Sign out</button></div>`;
  }
  function globalResults(q){ const x=q.toLowerCase(); const results=[]; state.orders.forEach(o=>{if([o.id,o.patient,o.vendor,o.orderedBy,o.status,...o.items].join(' ').toLowerCase().includes(x))results.push({type:'order',id:o.id,title:`${o.patient} · ${o.id}`,detail:`${o.vendor} · ${o.status}`})}); state.vendors.forEach(v=>{if([v.name,v.area,...v.categories].join(' ').toLowerCase().includes(x))results.push({type:'vendor',id:v.id,title:v.name,detail:`${v.area} · ${v.active} active orders`})}); return results.slice(0,6); }

  function viewMarkup(){
    if(state.role==='clinician') return clinicianView();
    if(state.view==='home') return homeView();
    if(state.view==='new') return newOrderView();
    if(state.view==='existing') return ordersView(false);
    if(state.view==='past') return ordersView(true);
    if(state.view==='vendors') return vendorsView();
    if(state.view==='reports') return reportsView();
    if(state.view==='messages') return messagesView();
    if(state.view==='settings') return settingsView();
    return '';
  }

  function homeView(){
    const active=state.orders.filter(o=>!['Complete'].includes(o.status)).length;
    const high=state.orders.filter(o=>o.risk>=50&&!['Complete'].includes(o.status)).length;
    const pending=state.approvals.filter(a=>a.status==='Pending' && (a.level===role().label || role().canOverride)).length;
    const roleFocus=state.role==='admission'?'Admissions, incomplete drafts and vendor verification':state.role==='charge'?'Team approvals, elevated risk and unfulfilled orders':state.role==='don'?'High-risk delivery, overrides and vendor quality':'Critical exceptions, network cost and executive approvals';
    return `${titleBlock('Action Center',`Good ${new Date().getHours()<12?'morning':'afternoon'}`,`${roleFocus}.`, `<button class="btn" data-action="save-draft">Save secure draft</button><button class="btn primary" data-view="new">+ New Order</button>`)}
      <section class="grid metrics"><div class="metric"><span>Active orders</span><strong>${active}</strong><small>Across ${new Set(state.orders.map(o=>o.vendor)).size} vendors</small></div><div class="metric"><span>High-risk orders</span><strong>${high}</strong><small>Rules-based and visible</small></div><div class="metric"><span>Approvals for you</span><strong>${pending}</strong><small>Deep-link ready</small></div><div class="metric"><span>Pickup queue</span><strong>${state.orders.filter(o=>o.status==='Pending Pickup').length}</strong><small>1 scheduled</small></div></section>
      ${['don','executive'].includes(state.role)?leadershipDashboard():''}
      ${high?`<section class="risk-callout"><div class="risk-score">${Math.max(...state.orders.map(o=>o.risk))}</div><div><span class="eyebrow">Highest fulfillment risk</span><h2>${state.orders.find(o=>o.risk===Math.max(...state.orders.map(x=>x.risk))).id} needs attention</h2><p>Item and timeline history plus a severe-weather disruption require vendor confirmation.</p></div><button class="btn dark" data-open-order="BRX-10458">Review order →</button></section>`:''}
      <div class="grid two"><section class="card"><div class="card-head"><div><h2>What needs attention</h2><p class="muted">Prioritized for ${role().label}</p></div></div><div class="list">${attentionRows()}</div></section>
      <section class="card"><div class="card-head"><div><h2>Approval inbox</h2><p class="muted">Cost, clinical and fulfillment exceptions</p></div></div>${approvalRows()}</section></div>
      <section class="card" style="margin-top:16px"><div class="card-head"><div><h2>Clinical shift handoff</h2><p class="muted">Patient and order context belongs to the organization—not one person’s shift.</p></div>${pill(state.handoff.acknowledged?'Acknowledged':'Needs acknowledgment',state.handoff.acknowledged?'green':'amber')}</div><div class="summary-strip"><div><span>Shift</span><strong>${state.handoff.shift}</strong></div><div><span>Prepared by</span><strong>${state.handoff.owner}</strong></div><div><span>Updated</span><strong>${state.handoff.updated}</strong></div></div><p>${esc(state.handoff.note)}</p><div class="actions"><button class="btn" data-action="edit-handoff">Add handoff note</button><button class="btn primary" data-action="ack-handoff" ${state.handoff.acknowledged?'disabled':''}>${state.handoff.acknowledged?'✓ Acknowledged':'Acknowledge handoff'}</button></div></section>`;
  }
  function attentionRows(){
    const rows=[];
    if(state.role==='admission') rows.push(['Draft 33','Nora P. · Approval requested','Continue draft','new']);
    if(state.role!=='admission') rows.push(['BRX-10458','Critical risk · Vendor not yet verified','Verify vendor','order']);
    rows.push(['BRX-10479','Delivered · Acknowledgment required','Review proof','order']);
    if(['don','executive'].includes(state.role)) rows.push(['ABC Home Medical','New positive quality event recorded','Review vendor','vendor']);
    return rows.map(([title,sub,action,type])=>`<div class="row"><div class="row-main"><strong>${title}</strong><small>${sub}</small></div><button class="btn small" ${type==='order'?`data-open-order="${title}"`:type==='vendor'?`data-open-vendor="abc"`:`data-view="new"`}>${action}</button></div>`).join('');
  }
  function approvalRows(){
    const visible=state.approvals.filter(a=>state.role==='executive'||a.level===role().label||(state.role==='don'&&a.level==='Case Manager')||(state.role==='charge'&&a.level==='Case Manager'));
    if(!visible.length) return `<div class="empty">No approvals assigned to this role.</div>`;
    return `<div class="list">${visible.map(a=>`<div class="row"><div class="row-main"><strong>${a.patient} · ${money(a.amount)}</strong><small>${a.order} · ${a.reason}</small></div><div class="row-meta">${pill(a.status,a.status==='Pending'?'amber':'green')}${a.status==='Pending'&&role().canApprove?`<button class="btn small" style="margin-top:6px" data-approve="${a.id}">Review</button>`:''}</div></div>`).join('')}</div>`;
  }

  function draftCost(){ const costs={'Hospital Bed E0260':540,'Oxygen Concentrator E1390':430,'Specialty Mattress E0277':750,'Wheelchair E1161':310,'Patient Lift E0630':820,'Suction Pump E0600':390}; return state.draft.items.reduce((s,x)=>s+(costs[x]||350),0); }
  function approvalLevel(cost){ return cost<=1000?'Admission Nurse':cost<=1500?'Case Manager':cost<=3000?'Director of Nursing':'Executive'; }
  function duplicateDetected(d){ return !!d.ehr && (d.items.includes('Oxygen Concentrator E1390') || !!d.duplicateResolution); }
  function duplicateUnresolved(d){ return duplicateDetected(d) && !d.duplicateResolution; }
  function duplicateExceptionNeedsApproval(d){ return d.duplicateResolution==='exception' && state.role==='admission'; }
  function duplicateLabel(value){ return ({remove:'Requested item removed',replace:'Replacement / exchange',not_duplicate:'Confirmed not a duplicate',exception:'Additional item clinically necessary'})[value]||'Unresolved'; }
  function escalationTarget(fromRole){ return fromRole==='charge'?'Director of Nursing':fromRole==='don'?'Clinician':fromRole==='executive'?'Clinician':'Director of Nursing'; }
  function riskForVendor(id){ const base={abc:46,mountain:25,front:59,summit:34}[id]||45; const time=Math.max(0,20-(Number(state.draft.timeline)||4)*3); const specialty=state.draft.items.some(i=>i.includes('Specialty'))?16:0; return Math.min(96,base+time+specialty); }
  function severeEventActive(d){ return d.severeEvent!==false; }
  function disruptionRequiresVerification(d){ return severeEventActive(d)&&Number(d.timeline)<8; }
  function vendorVerificationRequired(d,vendorId){ return riskForVendor(vendorId)>=50||disruptionRequiresVerification(d); }
  function sortedVendors(){ return [...state.vendors].filter(v=>state.draft.items.every(item=>v.categories.some(c=>itemMatchCategory(item,c))||v.id==='abc')).sort((a,b)=>hiddenRank(b)-hiddenRank(a)); }
  function itemMatchCategory(item,cat){ const i=item.toLowerCase(),c=cat.toLowerCase(); return (i.includes('bed')&&c.includes('bed'))||(i.includes('oxygen')&&c.includes('oxygen'))||(i.includes('wheel')&&c.includes('mobility'))||(i.includes('lift')&&c.includes('transfer'))||(i.includes('suction')&&c.includes('suction')); }
  function hiddenRank(v){ const negatives=v.events.filter(e=>e.type==='Negative').reduce((s,e)=>s+(e.severity==='High'?15:e.severity==='Moderate'?5:1),0); const positives=v.events.filter(e=>e.type==='Positive').length*2; const psfPenalty=(state.psfCases||[]).filter(f=>f.vendor===v.name).reduce((sum,f)=>sum+(f.status==='Recovered'?1:f.status==='Open'?8:4),0); return v.onTime*.55+(100-Math.min(v.active*3,35))*.15+positives-negatives-psfPenalty; }
  function newOrderView(){
    const d=state.draft,cost=draftCost(),level=approvalLevel(cost),dup=duplicateDetected(d); const chosen=state.vendors.find(v=>v.id===d.vendor)||state.vendors[0]; const risk=riskForVendor(chosen.id); const needsVerify=vendorVerificationRequired(d,chosen.id);
    return `${titleBlock('New Order','Create a DME order','EHR assists the draft. A clinician verifies every item before anything is submitted.',`<button class="btn" data-action="clear-draft">Clear</button><button class="btn ${d.savedLocal?'':'primary'}" data-action="save-draft">${d.savedLocal?'✓ Saved locally':'Save secure draft'}</button>`)}
      <div class="tabs">${[1,2,3,4].map((n)=>`<button class="${ui.newStage===n?'active':''}" data-stage="${n}">${n}. ${['Patient','Standing Orders','Vendor & Risk','Review'][n-1]}</button>`).join('')}</div>
      ${state.offline?`<div class="notice"><strong>◌ Offline mode</strong><p>This order can be saved securely on this device, but it has not reached a vendor. Submission is paused until the connection returns.</p></div>`:''}
      ${ui.newStage===1?`<section class="card"><div class="card-head"><div><h2>Patient and admission</h2><p class="muted">Pull available context into a clinician-reviewed draft, or select a pending admission below.</p></div><button class="btn primary" data-action="ehr-pull">${d.ehr?'✓ EHR data pulled':'Pull from EHR'}</button></div>${(state.pendingAdmits||[]).length?`<h3 style="margin:14px 0 9px">Admit pending patients</h3><div class="list">${state.pendingAdmits.map(p=>`<button class="row" data-admit-patient="${p.id}" style="width:100%;text-align:left"><div class="row-main"><strong>${esc(p.name)}</strong><small>${esc(p.mrn)} · ${esc(p.diagnosis)} · Admit ${esc(p.admitDate)}</small><small>${esc(p.location)} · ${esc(p.physician)}</small></div><div class="row-meta">${pill('Pending admit','amber')}</div></button>`).join('')}</div>`:''}<div class="form-grid" style="margin-top:14px"><div class="field"><label>Patient</label><input data-draft="patient" value="${esc(d.patient)}" placeholder="Search or select patient"></div><div class="field"><label>MRN</label><input value="${d.ehr?'MRN-88421':''}" readonly></div><div class="field full"><label>Delivery address</label><input data-draft="address" value="${esc(d.address)}"></div></div>${d.ehr?`<div class="success" style="margin-top:14px"><strong>✓ EHR context added to draft</strong><span class="tiny">Patient, admission, location, existing DME and standing-order candidates were retrieved. Nothing was submitted.</span></div>`:''}${dup?duplicateWorkflow(d):''}<div class="actions" style="margin-top:15px"><button class="btn primary" data-stage="2" ${duplicateUnresolved(d)?'disabled':''}>Continue</button></div></section>`:''}
      ${ui.newStage===2?standingStage(d):''}${ui.newStage===3?vendorStage(d,chosen,risk,needsVerify):''}${ui.newStage===4?reviewStage(d,cost,level,risk,needsVerify):''}`;
  }
  function duplicateWorkflow(d){
    if(d.duplicateResolution) return `<section class="success" style="margin-top:14px"><div class="card-head"><div><strong>✓ Duplicate flag resolved</strong><p class="tiny">Oxygen Concentrator E1390 · ${duplicateLabel(d.duplicateResolution)}</p></div>${pill('Audited','green')}</div><p>${esc(d.duplicateNote)}</p><p class="tiny muted">Resolved by ${esc(d.duplicateResolvedBy)} · ${esc(d.duplicateResolvedAt)}</p><button class="btn small" data-action="reopen-duplicate">Change resolution</button></section>`;
    const pending=d.duplicatePendingDisposition||'',pendingNote=d.duplicatePendingNote||'';
    return `<section class="notice" style="margin-top:14px"><div class="card-head"><div><strong>Duplicate / equipment conflict detected</strong><p>Oxygen Concentrator E1390 is already deployed for this patient.</p></div>${pill('Blocking','red')}</div><div class="summary-strip"><div><span>Existing equipment</span><strong>Oxygen Concentrator E1390</strong></div><div><span>Status</span><strong>Active at residence</strong></div><div><span>Vendor</span><strong>Summit Respiratory</strong></div><div><span>Last confirmed</span><strong>Aug 14 · 4:18 PM</strong></div></div><div class="form-grid"><div class="field full"><label>Choose a disposition</label><select id="duplicateDisposition" data-draft="duplicatePendingDisposition"><option value="" ${pending===''?'selected':''}>Select how to resolve</option><option value="remove" ${pending==='remove'?'selected':''}>Remove the new duplicate item</option><option value="replace" ${pending==='replace'?'selected':''}>Replace / exchange existing equipment</option><option value="not_duplicate" ${pending==='not_duplicate'?'selected':''}>Confirm this is not a duplicate</option><option value="exception" ${pending==='exception'?'selected':''}>Additional item is clinically necessary</option></select></div><div class="field full"><label>Resolution details</label><textarea id="duplicateNote" data-draft="duplicatePendingNote" rows="3" placeholder="Document what was verified, with whom, and why">${esc(pendingNote)}</textarea></div></div><p class="tiny muted">Replacement creates coordinated return activity. A clinically necessary additional item requires Case Manager authority when entered by an Admission Nurse.</p><button class="btn dark" style="margin-top:10px" data-action="resolve-duplicate">Resolve duplicate flag</button></section>`;
  }
  function standingStage(d){
    const allItems=['Hospital Bed E0260','Oxygen Concentrator E1390','Specialty Mattress E0277','Wheelchair E1161','Patient Lift E0630','Suction Pump E0600'];
    const cats=['Mobility','Beds / support surfaces','Oxygen / respiratory','Bathroom / safety','Patient transfer','Seating / positioning','Enteral / nutrition','Suction'];
    return `<section class="card"><div class="card-head"><div><h2>Standing Orders Verification</h2><p class="muted">Preauthorized areas are configured by the organization. Every selected item still requires clinician verification.</p></div>${pill(d.standing?'Verified':'Not verified',d.standing?'green':'amber')}</div>
      <h3 style="margin-bottom:9px">Saved admission bundles</h3><div class="check-list">${state.bundles.map(b=>`<button class="check" style="text-align:left" data-apply-bundle="${b.id}"><span><strong>${b.name}</strong><small class="muted">${b.items.join(' · ')}</small></span></button>`).join('')}</div><p class="tiny muted">Bundles speed selection only. Cost, duplicate, standing-order, benchmark and vendor-risk checks still run item by item.</p>
      <h3 style="margin-bottom:9px">Preauthorized categories</h3><div class="check-list">${cats.map(c=>`<label class="check"><input type="checkbox" data-category="${esc(c)}" ${state.preauthorized.includes(c)?'checked':''} ${permission('vendor')?'':'disabled'}><span><strong>${c}</strong><small class="muted">${state.preauthorized.includes(c)?'Organization approved':'Requires separate authorization'}</small></span></label>`).join('')}</div>
      <h3 style="margin:17px 0 9px">Order items</h3><div class="check-list">${allItems.map(i=>`<label class="check"><input type="checkbox" data-item="${esc(i)}" ${d.items.includes(i)?'checked':''}><span><strong>${i}</strong><small class="muted">${d.ehr&&['Hospital Bed E0260','Oxygen Concentrator E1390'].includes(i)?'EHR standing-order candidate':'Manually available'}</small></span></label>`).join('')}</div>
      <label class="check" style="margin-top:14px"><input type="checkbox" data-action="standing" ${d.standing?'checked':''}><span><strong>I verified the standing-order source, scope and selected items.</strong><small class="muted">This event records role, time, EHR/manual source and covered items.</small></span></label><div class="actions" style="margin-top:15px"><button class="btn" data-stage="1">Back</button><button class="btn primary" data-stage="3" ${d.standing?'':'disabled'}>Continue</button></div></section>`;
  }
  function vendorStage(d,chosen,risk,needsVerify){ const disruptionRule=disruptionRequiresVerification(d);
    return `<section class="grid two"><div class="card"><div class="card-head"><div><h2>Eligible vendors</h2><p class="muted">Order is deterministic. Ranking score stays hidden from normal users.</p></div></div><div class="list">${sortedVendors().map((v,i)=>{const r=riskForVendor(v.id);return `<button class="vendor-card ${d.vendor===v.id?'selected':''}" data-select-vendor="${v.id}"><div><h3>${i===0?pill('Best match','green'):''} ${v.name}</h3><p>${v.area} · ${v.onTime}% on-time · ${v.active} active</p><div class="risk-bar"><i class="${r>=50?'high':''}" style="width:${r}%"></i></div><p><strong>Order risk ${r} — ${r>=75?'Critical':r>=50?'High':r>=25?'Moderate':'Low'}</strong> · Based on these items and ${d.timeline}-hour timeline</p></div><span>Choose</span></button>`}).join('')}</div></div>
      <div><section class="card"><div class="card-head"><div><h2>Visible fulfillment risk</h2><p class="muted">Vendor + exact items + service vicinity + requested timeline</p></div>${pill(`${risk} · ${risk>=75?'Critical':risk>=50?'High':risk>=25?'Moderate':'Low'}`,riskClass(risk))}</div><div class="field"><label>Required delivery timeline</label><select data-draft="timeline"><option value="2" ${d.timeline==='2'?'selected':''}>Within 2 hours</option><option value="4" ${d.timeline==='4'?'selected':''}>Within 4 hours</option><option value="8" ${d.timeline==='8'?'selected':''}>Within 8 hours</option><option value="24" ${d.timeline==='24'?'selected':''}>Within 24 hours</option></select></div><div class="summary-strip"><div><span>Comparable success</span><strong>${Math.max(38,100-risk+12)}%</strong></div><div><span>Sample</span><strong>${chosen.id==='summit'?'9 · Limited':'64 orders'}</strong></div><div><span>Primary driver</span><strong>${d.items.some(i=>i.includes('Specialty'))?'Specialty mattress':'Timeline'}</strong></div></div></section>
      <section class="notice"><div class="card-head"><div><strong>⚠ Severe Weather / Disaster Monitor</strong><p>${esc(d.disruptionType||'Severe weather event')} intersects the patient service area and requested delivery window. Source: National Weather Service · Expires 6:00 PM. AI-assisted summary only; the trigger is based on official severity, geography and time.</p></div>${pill(disruptionRule?'Confirmation required':'Advisory',disruptionRule?'red':'amber')}</div><p class="tiny">Rule: every severe-weather or disaster event requires vendor confirmation when the requested delivery window is under 8 hours.</p></section>
      ${needsVerify?`<section class="card"><h3>Vendor availability verification</h3><p class="muted">Human confirmation is required for ${chosen.name}${disruptionRule?' because this under-8-hour order overlaps a severe event':risk>=50?' because fulfillment risk is elevated':''}.</p>${d.verified?`<div class="success"><strong>✓ Verified with Vendor</strong><span class="tiny">${role().label} · ${esc(d.vendorVerifyNote||'Availability and delivery commitment confirmed.')}</span></div><button class="btn small" style="margin-top:10px" data-action="open-vendor-contact">Review contact card</button>`:d.vendorVerificationFailed?`<div class="notice" style="border-color:#edbaba;background:var(--red-bg)"><strong>Vendor could not confirm every condition</strong><p>${esc(d.vendorVerifyNote)} Choose another eligible vendor or reopen the contact card after conditions change.</p></div><button class="btn" style="width:100%" data-action="open-vendor-contact">Update vendor response</button>`:`<div class="notice"><strong>Contact card requires completion</strong><p>Confirm equipment, timeline, service-area access and operating capacity directly with dispatch.</p></div><button class="btn primary" style="width:100%" data-action="open-vendor-contact">Open ${chosen.name} contact card</button>`}</section>`:''}</div></section><div class="actions" style="margin-top:15px"><button class="btn" data-stage="2">Back</button><button class="btn primary" data-stage="4" ${needsVerify&&!d.verified?'disabled':''}>Review order</button></div>`;
  }
  function reviewStage(d,cost,level,risk,needsVerify){ const duplicateApproval=duplicateExceptionNeedsApproval(d),within=cost<=role().limit&&!duplicateApproval,requiredLevel=duplicateApproval?'Case Manager':level; return `<section class="grid two"><div class="card"><h2>Order review</h2><div class="summary-strip"><div><span>Patient</span><strong>${esc(d.patient||'Patient required')}</strong></div><div><span>Vendor</span><strong>${esc((state.vendors.find(v=>v.id===d.vendor)||{}).name||'Not selected')}</strong></div><div><span>Timeline</span><strong>${d.timeline} hours</strong></div><div><span>Risk</span><strong>${risk}</strong></div></div>${d.duplicateResolution?`<div class="success" style="margin-bottom:14px"><strong>✓ Duplicate disposition: ${duplicateLabel(d.duplicateResolution)}</strong><p class="tiny">${esc(d.duplicateNote)} · ${esc(d.duplicateResolvedBy)} · ${esc(d.duplicateResolvedAt)}</p></div>`:''}<h3>Items</h3><div class="list">${d.items.map(i=>`<div class="row"><div class="row-main"><strong>${i}</strong><small>${state.preauthorized.some(c=>itemMatchCategory(i,c))?'Standing order — preauthorized':'Additional authorization required'}</small></div></div>`).join('')}</div><div class="field" style="margin-top:14px"><label>Clinical / order justification</label><textarea data-draft="notes" rows="3" placeholder="Required for exceptions">${esc(d.notes)}</textarea></div></div>
      <div><section class="card"><div class="card-head"><h2>Cost & duplicate governance</h2>${pill(within?'Within authority':'Approval required',within?'green':'amber')}</div><div class="summary-strip"><div><span>Projected 21-day cost</span><strong>${money(cost)}</strong></div><div><span>CMS/local benchmark</span><strong>${money(Math.round(cost*.86))}</strong></div><div><span>Variance</span><strong>+${money(Math.round(cost*.14))}</strong></div></div><p class="muted">Required authority: <strong>${requiredLevel}</strong>. Your authority: ${role().label} · ${role().limit===Infinity?'Enterprise':money(role().limit)}.</p>${duplicateApproval?`<div class="notice"><strong>Duplicate exception requires review</strong><p>An Admission Nurse may document the clinical need, but a Case Manager must authorize an additional same-item deployment.</p></div>`:!within?`<div class="notice"><strong>Escalation preserves this draft</strong><p>${state.role==='don'?'A 7-day or current-episode override may be documented.':state.role==='executive'?'A scoped override may be authorized for up to 30 days.':`Route to ${requiredLevel} without rebuilding the order.`}</p></div>`:''}</section>
      <section class="card" style="margin-top:15px"><h3>Submission status</h3><p class="muted">${state.offline?'Saved locally / Not yet submitted.':'Secure connection available. Submission creates a permanent timeline entry.'}</p><button class="btn primary" style="width:100%" data-action="submit-order" ${state.offline||!d.patient||!d.standing||duplicateUnresolved(d)||(needsVerify&&!d.verified)?'disabled':''}>${within?'Submit order':`Request ${requiredLevel} approval`}</button></section></div></section><div class="actions" style="margin-top:15px"><button class="btn" data-stage="3">Back</button></div>`; }

  function orderMatches(o,past){ const text=[o.id,o.patient,o.vendor,o.orderedBy,o.status,...o.items].join(' ').toLowerCase(); return (past?o.status==='Complete':o.status!=='Complete')&&text.includes(ui.orderSearch.toLowerCase())&&(ui.orderVendor==='All'||o.vendor===ui.orderVendor)&&(ui.orderOwner==='All'||o.orderedBy.includes(ui.orderOwner))&&(ui.orderStatus==='All'||o.status===ui.orderStatus); }
  function ordersView(past){ const orders=state.orders.filter(o=>orderMatches(o,past)); return `${titleBlock(past?'Closed lifecycle':'Operations',past?'Past Orders':'Existing Orders',past?'Completed, cancelled, returned and closed records with a unified timeline.':'Search active orders by vendor, ordered by, delivery status, patient, order number or equipment.',past?'':`<button class="btn primary" data-view="new">+ New Order</button>`)}
    <div class="filterbar"><div class="filter-field search"><label for="orderSearch">Search</label><input id="orderSearch" placeholder="Patient, order, vendor or equipment" value="${esc(ui.orderSearch)}"></div><div class="filter-field"><label for="orderVendor">Vendor</label><select id="orderVendor"><option>All</option>${[...new Set(state.orders.map(o=>o.vendor))].map(v=>`<option ${ui.orderVendor===v?'selected':''}>${v}</option>`).join('')}</select></div><div class="filter-field"><label for="orderOwner">Ordered by</label><select id="orderOwner"><option>All</option><option ${ui.orderOwner==='A. Smith'?'selected':''}>A. Smith</option><option ${ui.orderOwner==='C. Lee'?'selected':''}>C. Lee</option><option ${ui.orderOwner==='J. Perez'?'selected':''}>J. Perez</option></select></div><div class="filter-field"><label for="orderStatus">Delivery status</label><select id="orderStatus"><option>All</option>${['Dispatched','Delivered','Pending Pickup','Complete'].map(s=>`<option ${ui.orderStatus===s?'selected':''}>${s}</option>`).join('')}</select></div></div>
    <section class="card"><div class="list">${orders.length?orders.map(orderRow).join(''):`<div class="empty">No orders match these filters.</div>`}</div></section>`; }
  function orderRow(o){ const delivery=deliveryEta(o),pickupWindow=pickupEta(o); return `<button class="row ${o.risk>=50?'high':''}" data-open-order="${o.id}" style="width:100%;text-align:left"><div class="avatar">${o.patient.split(' ').map(x=>x[0]).join('')}</div><div class="row-main"><strong>${esc(o.patient)} · ${esc(o.id)}</strong><small>${esc(o.vendor)} · Ordered by ${esc(o.orderedBy)}</small><small>${esc(o.items.join(' · '))}</small></div><div class="row-meta">${pill(o.status,statusClass(o.status))} ${pill(`Risk ${o.risk}`,riskClass(o.risk))}${delivery?`<small class="eta-label"><strong>Delivery ETA</strong>${esc(delivery)}</small>`:''}${pickupWindow?`<small class="eta-label pickup"><strong>Pickup ETA</strong>${esc(pickupWindow)}</small>`:''}</div></button>`; }

  function orderDrawer(){ const o=state.orders.find(x=>x.id===state.selectedOrder); if(!o)return''; const canMessage=permission('message')&&o.status==='Dispatched'; return `<><div class="drawer-backdrop" data-action="close-order"></div><aside class="drawer"><div class="drawer-head"><div><span class="eyebrow">${o.id}</span><h2>${o.patient}</h2><div>${pill(o.status,statusClass(o.status))} ${pill(`Risk ${o.risk} · ${o.riskLabel}`,riskClass(o.risk))}</div></div><button class="close" data-action="close-order">×</button></div>
      ${permission('exact')?`<section class="card"><div class="card-head"><div><h3>Exact lifecycle status</h3><p class="muted">${o.exact}</p></div></div>${o.status==='Dispatched'?`<div class="map"><span class="truck">▰ ${o.location}</span></div><strong>Delivery ETA: ${esc(deliveryEta(o))}</strong><p class="tiny muted">Location sharing stops when delivery is complete. Last vendor update 2:04 PM.</p>`:''}${pickupEta(o)?`<div class="summary-strip"><div><span>Pickup status</span><strong>${esc(o.pickup)}</strong></div><div><span>Pickup ETA</span><strong>${esc(pickupEta(o))}</strong></div></div>`:''}</section>`:`<section class="card"><h3>Operational status</h3><p>${o.status}${deliveryEta(o)?` · Delivery ETA: ${esc(deliveryEta(o))}`:''}${pickupEta(o)?` · Pickup ETA: ${esc(pickupEta(o))}`:''}</p><p class="tiny muted">Exact lifecycle, proof and attestation are visible to DON and Executive roles.</p></section>`}
      <section class="card" style="margin-top:14px"><div class="card-head"><h3>Order details</h3><strong>${money(o.cost)}</strong></div><div class="summary-strip"><div><span>Vendor</span><strong>${o.vendor}</strong></div><div><span>CMS/local benchmark</span><strong>${money(o.benchmark)}</strong></div><div><span>Ordered by</span><strong>${o.orderedBy.split(' · ')[0]}</strong></div></div>${o.duplicateDisposition?`<div class="success" style="margin-bottom:12px"><strong>✓ Duplicate flag: ${duplicateLabel(o.duplicateDisposition)}</strong><span class="tiny">${esc(o.duplicateNote||'Resolution retained in audit history.')}</span></div>`:''}<div class="list">${o.items.map(i=>`<div class="row"><div class="row-main"><strong>${i}</strong>${deliveryEta(o)?`<small><strong>Delivery ETA:</strong> ${esc(deliveryEta(o))}</small>`:''}${pickupEta(o)?`<small><strong>Pickup ETA:</strong> ${esc(pickupEta(o))}</small>`:''}</div></div>`).join('')}</div><div class="actions" style="margin-top:12px"><button class="btn small" data-action="modify-order">Modify order</button>${o.vendorSplit?`<div class="notice" style="margin-top:10px"><strong>Split Delivery</strong><p>This order has been split across multiple deliveries. Reason: ${esc(o.splitReason||'Vendor-initiated split')}</p></div>`:''}${state.role==='charge'&&o.items.length>1&&!o.splitAllocation?`<button class="btn small" style="margin-top:6px" data-action="request-split-allocation" data-split-order="${o.id}">Request Split Allocation</button>`:''}<button class="btn small danger" data-action="cancel-order" ${o.status==='Complete'?'disabled':''}>Cancel</button><button class="btn small" data-action="pickup-order">${o.status==='Pending Pickup'?'Advance pickup':'Request pickup'}</button></div></section>
      <section class="card" style="margin-top:14px"><div class="card-head"><h3>Order timeline</h3></div><div class="timeline">${o.timeline.map((t,i)=>`<div class="timeline-item ${i===o.timeline.length-1?'current':'done'}"><span class="timeline-dot">${i===o.timeline.length-1?'•':'✓'}</span><div><strong>${esc(t.split(' · ')[0])}</strong><small>${esc(t.includes(' · ')?t.split(' · ').slice(1).join(' · '):'Audited event')}</small></div></div>`).join('')}</div></section>
      ${o.delivered&&permission('exact')?`<section class="card" style="margin-top:14px"><div class="card-head"><h3>Proof & condition attestation</h3>${pill('Vendor signed','green')}</div><div class="proof-grid"><div class="proof">▧<br>Delivery photo<br><small>GPS · 11:46 AM</small></div><div class="proof">✍<br>Recipient signature<br><small>Jordan M. family</small></div><div class="proof">▧<br>Condition photo<br><small>No visible damage</small></div><div class="proof">✓<br>Setup complete<br><small>Correct equipment confirmed</small></div></div>${!o.acknowledged?`<button class="btn primary" style="width:100%;margin-top:12px" data-action="ack-delivery">Acknowledge delivery</button>`:`<div class="success" style="margin-top:12px"><strong>✓ Delivery acknowledged</strong><span class="tiny">Message thread automatically archived for audit.</span></div>`}</section>`:''}
      ${canMessage?`<section class="card" style="margin-top:14px"><div class="card-head"><h3>In-app delivery messaging</h3>${pill('Active delivery','green')}</div>${chatMarkup(o)}</section>`:''}
      ${!canMessage&&o.archivedMessages?`<section class="card" style="margin-top:14px"><h3>Communications</h3><p class="muted">${permission('archive')?'Archived thread available in Messages → Archive.':'Archived after delivery acknowledgment. Executive access only.'}</p></section>`:''}
    </aside></>`; }
  function chatMarkup(o){ return `<div class="chat"><div class="messages">${o.messages.length?o.messages.map(m=>`<div class="bubble ${m.me?'me':''}">${esc(m.text)}<small>${m.me?role().label:'Delivery person'} · ${m.time}</small></div>`).join(''):`<div class="empty">No messages yet.</div>`}</div><div class="chat-compose"><input id="chatInput" placeholder="Message delivery person"><button class="btn primary" data-action="send-chat">Send</button></div></div>`; }

  function vendorsView(){ if(!permission('vendor')) return `${titleBlock('Role-based access','Vendor Management','Vendor profiles and editable capabilities are available to Director of Nursing and Executive users.')}<div class="empty">Switch to Director of Nursing or Executive to manage the vendor network.</div>`; return `${titleBlock('Network','Vendor Management','Current contracted vendors are ordered by a hidden, deterministic rules engine. No score is shown to provider or vendor users.',`<button class="btn">Invite vendor</button>`)}<section class="card"><div class="list">${[...state.vendors].sort((a,b)=>hiddenRank(b)-hiddenRank(a)).map(v=>`<button class="row" data-open-vendor="${v.id}" style="width:100%;text-align:left"><span class="avatar">${v.name.split(' ').slice(0,2).map(x=>x[0]).join('')}</span><div class="row-main"><strong>${v.name}</strong><small>${v.area}</small><small>${v.categories.join(' · ')}</small></div><div class="row-meta">${pill(`${v.onTime}% on time`,'green')}<small>${v.active} current orders</small></div></button>`).join('')}</div></section>`; }
  function vendorDrawer(){ const v=state.vendors.find(x=>x.id===state.selectedVendor); if(!v)return''; const tabs=['profile','items','orders','quality']; return `<><div class="drawer-backdrop" data-action="close-vendor"></div><aside class="drawer"><div class="drawer-head"><div><span class="eyebrow">Current vendor</span><h2>${v.name}</h2><p class="muted">${v.area}</p></div><button class="close" data-action="close-vendor">×</button></div><div class="tabs">${tabs.map(t=>`<button class="${ui.vendorTab===t?'active':''}" data-vendor-tab="${t}">${t[0].toUpperCase()+t.slice(1)}</button>`).join('')}</div>
      ${ui.vendorTab==='profile'?`<section class="card"><h3>Demographics</h3><div class="summary-strip"><div><span>Address</span><strong>${v.address}</strong></div><div><span>Hours</span><strong>${v.hours}</strong></div><div><span>Network status</span><strong>Active</strong></div></div><h3>Points of contact</h3><p>${v.contacts}</p></section>`:''}
      ${ui.vendorTab==='items'?`<section class="card"><div class="card-head"><div><h3>Items provided</h3><p class="muted">Seeded from network signup; edits are timestamped and audited.</p></div></div><div class="check-list">${['Beds / support surfaces','Oxygen / respiratory','Mobility','Bathroom / safety','Patient transfer','Suction','Ventilation'].map(c=>`<label class="check"><input type="checkbox" data-vendor-category="${esc(c)}" ${v.categories.includes(c)?'checked':''}><span>${c}</span></label>`).join('')}</div></section>`:''}
      ${ui.vendorTab==='orders'?`<section class="card"><h3>Current and past orders</h3><div class="list">${state.orders.filter(o=>o.vendor===v.name).map(orderRow).join('')||'<div class="empty">No matching orders.</div>'}</div></section>`:''}
      ${ui.vendorTab==='quality'?`<section class="card"><div class="card-head"><div><h3>Quality events</h3><p class="muted">Feedback changes background ranking through explicit rules, severity weights, recency and volume normalization.</p></div><button class="btn primary small" data-action="quality-event">+ Create Quality Event</button></div><div class="list">${v.events.map(e=>`<div class="row"><div class="row-main"><strong>${e.type} · ${e.category}</strong><small>${e.note}</small></div><div class="row-meta">${pill(e.severity,e.type==='Negative'?'red':'green')}<small>${e.date}</small></div></div>`).join('')||'<div class="empty">No quality events recorded.</div>'}</div></section>`:''}
    </aside></>`; }
  function qualityModal(){ return `<><div class="modal-backdrop" data-action="close-quality"></div><div class="modal"><div class="drawer-head"><div><span class="eyebrow">Audited feedback</span><h2>Create Quality Event</h2></div><button class="close" data-action="close-quality">×</button></div><div class="form-grid"><div class="field"><label>Type</label><select id="qeType"><option>Positive</option><option>Negative</option></select></div><div class="field"><label>Severity</label><select id="qeSeverity"><option>Low</option><option>Moderate</option><option>High</option></select></div><div class="field full"><label>Category</label><select id="qeCategory"><option>Delivery timeliness</option><option>Communication</option><option>Equipment availability</option><option>Order accuracy</option><option>Equipment condition</option><option>Pickup responsiveness</option><option>Issue resolution</option><option>Exceptional service</option></select></div><div class="field full"><label>Details</label><textarea id="qeNote" rows="4" placeholder="Describe the observed event and impact"></textarea></div></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="save-quality">Save quality event</button></div></>`; }

  function reportsView(){
    const scope=role().reports,leadership=['don','executive'].includes(state.role);
    const base=titleBlock('Insights','Reports',scope==='executive'?'Enterprise revenue, DME cost, contribution margin, PSF recovery and network performance.':scope==='clinical'?'Clinical operations, preventable service recovery, vendor quality and financial impact.':'Team order, delivery and pickup activity.');
    if(leadership) return `${base}${leadershipDashboard()}<div class="grid two governance-grid"><section class="card"><h2>Cost controls</h2><div class="summary-strip"><div><span>Episode spend</span><strong>$86,220</strong></div><div><span>Cost / patient-day</span><strong>$18.42</strong></div><div><span>Avoidable spend</span><strong>$2,180</strong></div><div><span>Projected remaining</span><strong>$31,600</strong></div></div><div class="notice"><strong>Long-stay governance</strong><p>Day 14 early review · Day 21 ongoing mode · Day 30 DON review · Day 60 Executive review · 90+ periodic reconciliation.</p></div></section><section class="card"><h2>Ranking and benchmark rules</h2><p class="muted">Vendor ranking remains hidden in ordering workflows but is reproducible for authorized governance review.</p><div class="list"><div class="row"><div class="row-main"><strong>Eligibility and service-area match</strong><small>Required prerequisite</small></div></div><div class="row"><div class="row-main"><strong>Item/timeline and dispatch coverage</strong><small>Recent outcomes plus weekday, weekend and holiday availability</small></div></div><div class="row"><div class="row-main"><strong>Cost and CMS/local benchmark</strong><small>Actual variance and trend magnitude</small></div></div><div class="row"><div class="row-main"><strong>Quality and PSF recovery</strong><small>Defined severity, recurrence and recovery weights</small></div></div></div></section></div>`;
    return `${base}<section class="grid metrics"><div class="metric"><span>21-day DME spend</span><strong>$38.4K</strong><small>4.8% below benchmark</small></div><div class="metric"><span>On-time delivery</span><strong>94%</strong><small>+3.2 pts this month</small></div><div class="metric"><span>Open pickups</span><strong>6</strong><small>1 overdue</small></div><div class="metric"><span>Verified disruptions</span><strong>8</strong><small>100% human confirmed</small></div></section>`;
  }

  function messagesView(){ if(ui.messageFilter==='Archive'&&!permission('archive')) ui.messageFilter='Active'; const archive=ui.messageFilter==='Archive'; const available=state.orders.filter(o=>archive?o.archivedMessages:!o.archivedMessages&&o.messages.length); const current=available.find(o=>o.id===ui.messageOrder)||available[0]; return `${titleBlock('Order-linked communication','Messages','In-app delivery conversations remain attached to the order, vendor and delivery person.')}<div class="tabs"><button class="${ui.messageFilter==='Active'?'active':''}" data-message-filter="Active">Active</button><button class="${ui.messageFilter==='Unread'?'active':''}" data-message-filter="Unread">Unread</button><button class="${ui.messageFilter==='Escalations'?'active':''}" data-message-filter="Escalations">Escalations</button>${permission('archive')?`<button class="${ui.messageFilter==='Archive'?'active':''}" data-message-filter="Archive">Archive</button>`:''}</div>${!permission('message')?`<div class="empty">Direct delivery messaging is available to Director of Nursing, Executive and Clinician roles. Order notifications remain visible in your Action Center.</div>`:`<div class="grid two"><section class="card"><div class="list">${available.map(o=>`<button class="row" data-message-order="${o.id}" style="width:100%;text-align:left"><div class="row-main"><strong>${o.patient} · ${o.id}</strong><small>${o.vendor} · ${archive?'Archived after acknowledgment':o.status}</small></div>${pill(archive?'Archived':'Active',archive?'':'green')}</button>`).join('')||'<div class="empty">No conversations in this view.</div>'}</div></section><section class="card">${current?`<div class="card-head"><div><h2>${current.patient}</h2><p class="muted">${current.id} · ${current.vendor}</p></div>${archive?pill('Read-only archive','amber'):pill('Active delivery','green')}</div>${archive?`<div class="empty">Archived audit thread · sender, recipient, timestamps, delivery/read state and delivery-person identity retained.</div>`:chatMarkup(current)}`:'<div class="empty">Select a conversation.</div>'}</section></div>`}`; }

  function settingsView(){ const p=state.preferences,cp=state.costPolicy; return `${titleBlock('Personal controls','Notification & security settings','Choose how operational events reach you. Critical alerts always bypass quiet hours.',`<button class="btn" data-view="home">← Home</button>`)}<div class="grid two"><section class="card"><div class="card-head"><div><h2>Notification channels</h2><p class="muted">System notifications use minimal patient information by default.</p></div></div><div class="list"><label class="check"><input type="checkbox" data-pref="inApp" ${p.inApp?'checked':''}><span><strong>In-app notifications</strong><small class="muted">Approvals, delivery, risk and pickup events</small></span></label><label class="check"><input type="checkbox" data-pref="push" ${p.push?'checked':''}><span><strong>Mobile push notifications</strong><small class="muted">Actionable deep links after secure re-entry</small></span></label><label class="check"><input type="checkbox" data-pref="criticalAlways" ${p.criticalAlways?'checked':''}><span><strong>Critical alerts bypass quiet hours</strong><small class="muted">Critical risk, failed delivery and severe disruption</small></span></label><label class="check"><input type="checkbox" data-pref="phiPreview" ${p.phiPreview?'checked':''}><span><strong>Show patient details in OS previews</strong><small class="muted">Off is recommended for mobile privacy</small></span></label></div><div class="form-grid" style="margin-top:14px"><div class="field"><label>Quiet hours start</label><input type="time" data-pref-value="quietStart" value="${p.quietStart}"></div><div class="field"><label>Quiet hours end</label><input type="time" data-pref-value="quietEnd" value="${p.quietEnd}"></div></div></section><section class="card"><div class="card-head"><div><h2>Event subscriptions</h2><p class="muted">Your role still determines what data you may open.</p></div></div><div class="list"><label class="check"><input type="checkbox" data-pref="cost" ${p.cost?'checked':''}><span>Cost approvals and overrides</span></label><label class="check"><input type="checkbox" data-pref="risk" ${p.risk?'checked':''}><span>Risk increases and disruption verification</span></label><label class="check"><input type="checkbox" data-pref="delivery" ${p.delivery?'checked':''}><span>Dispatch, ETA and delivery acknowledgment</span></label><label class="check"><input type="checkbox" data-pref="pickup" ${p.pickup?'checked':''}><span>Pickup requested, scheduled, overdue and complete</span></label></div><div class="success" style="margin-top:14px"><strong>✓ Mobile security defaults active</strong><span class="tiny">Biometric re-entry · automatic session lock · encrypted local drafts · remote session revocation ready · sensitive cache clears on logout.</span></div></section></div>${permission('vendor')?`<section class="card" style="margin-top:16px"><div class="card-head"><div><h2>Organization cost policy</h2><p class="muted">DON and Executive users can adjust role authority. Changes affect New Order and Approval Inbox immediately.</p></div>${pill('Audited configuration','blue')}</div><div class="form-grid"><div class="field"><label>Admission Nurse limit</label><input type="number" min="0" step="50" data-policy="admission" value="${cp.admission}"></div><div class="field"><label>Case Manager limit</label><input type="number" min="0" step="50" data-policy="charge" value="${cp.charge}"></div><div class="field"><label>DON limit</label><input type="number" min="0" step="50" data-policy="don" value="${cp.don}"></div><div class="field"><label>DON override duration (days)</label><input type="number" min="1" max="30" data-policy="donDays" value="${cp.donDays}"></div><div class="field"><label>Executive override maximum (days)</label><input type="number" min="1" max="90" data-policy="executiveDays" value="${cp.executiveDays}"></div></div></section>`:''}`; }

  function actionModal(){
    const o=state.orders.find(x=>x.id===ui.modalId),a=state.approvals.find(x=>x.id===ui.modalId),psf=state.psfCases.find(x=>x.id===ui.modalId);
    if(ui.actionModal==='vendor_verify'){
      const v=state.vendors.find(x=>x.id===state.draft.vendor)||state.vendors[0],risk=riskForVendor(v.id),riskBand=risk>=75?'Critical':risk>=50?'High':risk>=25?'Moderate':'Low',disruptionRule=disruptionRequiresVerification(state.draft),phone=v.contacts.split(' · ').slice(-1)[0],email=v.email||({abc:'dispatch@abchomemedical.demo',mountain:'dispatch@mountaindme.demo',front:'dispatch@frontrangemedical.demo',summit:'dispatch@summitrespiratory.demo'}[v.id]||'dispatch@vendor.demo');
      return modalShell('Vendor Availability Verification',`${v.name} · Risk ${risk} — ${riskBand}`,`<div class="notice"><strong>⚠ Contact required before submission</strong><p>${disruptionRule?`${esc(state.draft.disruptionType||'Severe weather or disaster event')} overlaps this under-8-hour order. Confirmation is required regardless of the historical risk score.`:`The requested items and ${state.draft.timeline}-hour timeline create elevated fulfillment risk.`}</p></div><div class="summary-strip"><div><span>Primary dispatch</span><strong>${esc(v.contacts.split(' · ')[0])}</strong></div><div><span>Phone</span><strong>${esc(phone)}</strong></div><div><span>Dispatch email</span><strong>${esc(email)}</strong></div><div><span>Expected rental length</span><strong>${Number(state.draft.rentalDays)||21} days</strong></div></div><div class="actions" style="margin-bottom:14px"><button class="btn primary" data-action="copy-vendor-phone">Copy phone number</button><button class="btn" data-action="copy-vendor-email">Copy dispatch email</button></div><h3 style="margin-bottom:9px">Items ordered</h3><div class="list">${state.draft.items.map(i=>`<div class="row"><div class="row-main"><strong>${esc(i)}</strong><small>Expected rental: ${Number(state.draft.rentalDays)||21} days</small></div></div>`).join('')}</div><h3 style="margin:16px 0 9px">Vendor confirmations</h3><div class="form-grid"><div class="field"><label>All ordered equipment available?</label><select id="verifyEquipment"><option value="">Choose Yes or No</option><option value="yes">Yes</option><option value="no">No</option></select></div><div class="field"><label>Requested delivery timeline confirmed?</label><select id="verifyTimeline"><option value="">Choose Yes or No</option><option value="yes">Yes</option><option value="no">No</option></select></div><div class="field"><label>Address and service area accessible?</label><select id="verifyAddress"><option value="">Choose Yes or No</option><option value="yes">Yes</option><option value="no">No</option></select></div><div class="field"><label>No known operational disruption?</label><select id="verifyOperations"><option value="">Choose Yes or No</option><option value="yes">Yes</option><option value="no">No</option></select></div></div><div class="field" style="margin-top:14px"><label>Required comments</label><textarea id="vendorVerifyNote" rows="3" placeholder="Who confirmed availability and any delivery conditions, notes, etc."></textarea></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="confirm-vendor-contact">Record vendor response</button>`);
    }
    if(ui.actionModal==='psf-create') return modalShell('Record Preventable Service Failure','Available to Director of Nursing and Executive roles.',`<div class="form-grid"><div class="field"><label for="newPsfVendor">Vendor</label><select id="newPsfVendor">${state.vendors.map(v=>`<option>${esc(v.name)}</option>`).join('')}</select></div><div class="field"><label for="newPsfOrder">Order</label><select id="newPsfOrder">${state.orders.map(x=>`<option>${esc(x.id)}</option>`).join('')}</select></div><div class="field full"><label for="newPsfFailure">Failure category</label><select id="newPsfFailure"><option>Missed dispatch commitment</option><option>Incorrect or missing equipment</option><option>Late delivery</option><option>Pickup delay / excess rental</option><option>Proof or billing documentation failure</option><option>Communication failure</option><option>Other preventable failure</option></select></div><div class="field"><label for="newPsfExposure">Revenue at risk</label><input id="newPsfExposure" type="number" min="1" step="1" placeholder="0"></div><div class="field"><label for="newPsfStatus">Initial status</label><select id="newPsfStatus"><option>Open</option><option>Under Review</option></select></div><div class="field full"><label for="newPsfNote">Required details</label><textarea id="newPsfNote" rows="4" placeholder="Describe why the failure was preventable and its operational or revenue impact"></textarea></div></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="save-new-psf">Create PSF record</button>`);
    if(ui.actionModal==='psf-recovery'&&psf) return modalShell('Record PSF recovery',`${psf.id} · ${psf.vendor} · ${psf.order}`,`<div class="notice"><strong>${esc(psf.failure)}</strong><p>Document recovered revenue only when the operational or billing recovery is confirmed.</p></div><div class="summary-strip"><div><span>Revenue at risk</span><strong>${money(psf.revenueAtRisk)}</strong></div><div><span>Current status</span><strong>${esc(psf.status)}</strong></div></div><div class="form-grid"><div class="field"><label for="psfRecoveryAmount">Revenue recovered</label><input id="psfRecoveryAmount" type="number" min="0" max="${psf.revenueAtRisk}" step="1" value="${psf.revenueAtRisk}"></div><div class="field"><label for="psfRecoveryOutcome">Recovery outcome</label><select id="psfRecoveryOutcome"><option>Vendor credit confirmed</option><option>Claim / billing hold resolved</option><option>Replacement delivery retained revenue</option><option>Rental overage reversed</option><option>Other recovery</option></select></div><div class="field full"><label for="psfRecoveryNote">Required recovery notes</label><textarea id="psfRecoveryNote" rows="4" placeholder="Document the recovery source, confirmation and financial impact"></textarea></div></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="save-psf-recovery">Close PSF and record recovery</button>`);
    if(ui.actionModal==='modify'&&o) return modalShell('Modify order',`Changes after dispatch notify ${o.vendor} and create an audit entry.`,`<div class="form-grid"><div class="field"><label>Delivery priority</label><select id="modifyPriority"><option>Same-day routine</option><option selected>Urgent · within 4 hours</option><option>Critical · within 2 hours</option></select></div><div class="field"><label>Delivery address</label><input id="modifyAddress" value="1452 East Willow Lane, Salt Lake City, UT"></div><div class="field full"><label>Equipment changes</label><textarea id="modifyItems" rows="3">${esc(o.items.join('\n'))}</textarea></div><div class="field full"><label>Clinical reason</label><textarea id="modifyReason" rows="3" placeholder="Required for every post-submission change"></textarea></div></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="save-modification">Submit modification</button>`);
    if(ui.actionModal==='cancel'&&o) return modalShell('Cancel order',`Cancellation will notify ${o.vendor} and remain visible in the timeline.`,`<div class="field"><label>Cancellation reason</label><select id="cancelReason"><option>Clinical need changed</option><option>Duplicate order</option><option>Patient location changed</option><option>Vendor unable to fulfill</option><option>Other</option></select></div><div class="field" style="margin-top:12px"><label>Required details</label><textarea id="cancelNote" rows="4" placeholder="Document who confirmed the cancellation and why"></textarea></div><button class="btn danger" style="width:100%;margin-top:15px" data-action="confirm-cancel">Cancel order and notify vendor</button>`);
    if(ui.actionModal==='pickup'&&o) return modalShell('Pickup management',`Current pickup state: ${o.pickup}.`,`<div class="summary-strip"><div><span>Current pickup ETA</span><strong>${esc(pickupEta(o)||'Not set')}</strong></div></div><div class="form-grid"><div class="field"><label>Pickup reason</label><select id="pickupReason"><option>Patient discharge</option><option>Patient death</option><option>Equipment exchange</option><option>No longer needed</option></select></div><div class="field"><label>Requested pickup ETA / window</label><select id="pickupWindow"><option>Today · 2–5 PM</option><option>Tomorrow · 8–12 PM</option><option>Tomorrow · 12–5 PM</option></select></div><div class="field full"><label>Return / family notes</label><textarea id="pickupNote" rows="3" placeholder="Access instructions, family preferences or condition notes"></textarea></div></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="confirm-pickup">${o.status==='Pending Pickup'?'Advance pickup status':'Request pickup'}</button>`);
    if(ui.actionModal==='approval'&&a){ const executive=state.role==='executive',don=state.role==='don'; return modalShell('Approval & override review',`${a.patient} · ${a.order} · ${money(a.amount)}`,`<div class="notice"><strong>Reason for escalation</strong><p>${esc(a.reason)}</p></div><div class="summary-strip"><div><span>Requested by</span><strong>${a.requester}</strong></div><div><span>Required level</span><strong>${a.level}</strong></div><div><span>Current status</span><strong>${a.status}</strong></div></div><div class="form-grid"><div class="field"><label>Decision</label><select id="approvalDecision"><option>Approve</option><option>Deny</option><option>Escalate</option></select></div><div class="field"><label>Scope</label><select id="approvalScope"><option>This order</option>${don||executive?'<option>Current patient episode</option>':''}${executive?'<option>This vendor/category for this facility</option>':''}</select></div><div class="field"><label>Expiration</label><select id="approvalExpiry"><option>Order completion</option>${don?`<option>${state.costPolicy.donDays} days</option>`:''}${executive?`<option>7 days</option><option>${state.costPolicy.executiveDays} days</option>`:''}</select></div><div class="field full"><label>Decision rationale</label><textarea id="approvalNote" rows="3" placeholder="Required for approval, denial or override"></textarea></div></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="save-approval">Record decision</button>`); }
    if(ui.actionModal==='handoff') return modalShell('Add clinical handoff note','This note remains attached to the organization’s operational context across shifts.',`<div class="field"><label>Handoff note</label><textarea id="handoffNote" rows="5">${esc(state.handoff.note)}</textarea></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="save-handoff">Save handoff note</button>`);
    if(ui.actionModal==='vendor-proof'){const o=state.orders.find(x=>x.id===ui.modalId);if(!o)return '';return modalShell('Submit Proof of Delivery',o.id+' · '+o.patient,`<div class="summary-strip"><div><span>Photo</span><strong>▧ Photo placeholder</strong></div><div><span>GPS</span><strong>40.7608, -111.8910</strong></div><div><span>Timestamp</span><strong>${new Date().toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})}</strong></div></div><div class="field" style="margin-top:14px"><label>Recipient signature</label><div style="border:2px dashed #ccc;padding:30px;text-align:center;border-radius:8px;color:#999">✍ Signature capture area</div></div><div class="field" style="margin-top:12px"><label>Delivery notes</label><textarea id="proofNotes" rows="3" placeholder="Equipment setup confirmed, condition documented"></textarea></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="confirm-vendor-proof">Submit Delivery Proof</button>`);}
    if(ui.actionModal==='vendor-split'){const o=state.orders.find(x=>x.id===ui.modalId);if(!o)return '';return modalShell('Split Fulfillment','Select items for split delivery and provide a reason.',`<div class="check-list">${o.items.map((item,idx)=>`<label class="check"><input type="checkbox" data-split-item="${idx}" checked><span>${esc(item)}</span></label>`).join('')}</div><div class="field" style="margin-top:14px"><label>Reason for split</label><textarea id="splitReason" rows="3" placeholder="Explain why fulfillment must be split (e.g., partial stock, equipment at different warehouses)"></textarea></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="confirm-vendor-split">Confirm Split Fulfillment</button>`);}
    if(ui.actionModal==='split-allocation'){const o=state.orders.find(x=>x.id===ui.modalId);if(!o)return '';const vendors=state.vendors;return modalShell('Request Split Allocation','Items not available from a single vendor. Request DON approval to split across vendors.',`<div class="list">${o.items.map((item,idx)=>`<div class="row"><div class="row-main"><strong>${esc(item)}</strong></div><div class="row-meta"><select data-alloc-vendor="${idx}">${vendors.map(v=>`<option value="${v.id}">${esc(v.name)}</option>`).join('')}</select></div></div>`).join('')}</div><div class="field" style="margin-top:14px"><label>Clinical justification</label><textarea id="splitAllocReason" rows="3" placeholder="Document why a single vendor cannot fulfill all items"></textarea></div><button class="btn primary" style="width:100%;margin-top:15px" data-action="confirm-split-allocation">Submit for DON Approval</button>`);}
    return '';
  }
  function modalShell(title,body,content){ return `<><div class="modal-backdrop" data-action="close-action-modal"></div><div class="modal"><div class="drawer-head"><div><span class="eyebrow">Audited workflow</span><h2>${title}</h2><p class="muted">${body}</p></div><button class="close" data-action="close-action-modal">×</button></div>${content}</div></>`; }

  function bindInputValue(target){ const key=target.dataset.draft; if(!key)return; state.draft[key]=target.value; if(key==='timeline'){state.draft.verified=false;state.draft.vendorVerificationFailed=false;state.draft.vendorVerifyNote='';state.draft.vendorVerificationResponses=null;if(vendorVerificationRequired(state.draft,state.draft.vendor)){ui.actionModal='vendor_verify';ui.modalId=state.draft.vendor;}} state.draft.savedLocal=false; save(); }

  document.addEventListener('input',e=>{
    const t=e.target;
    if(t.id==='globalSearch'){ui.globalSearch=t.value;render();const input=document.getElementById('globalSearch');if(input){input.focus();input.setSelectionRange(input.value.length,input.value.length);}}
    else if(t.id==='orderSearch'){ui.orderSearch=t.value;render();const input=document.getElementById('orderSearch');if(input){input.focus();input.setSelectionRange(input.value.length,input.value.length);}}
    else bindInputValue(t);
  });
  document.addEventListener('change',e=>{
    const t=e.target;
    if(t.id==='enrollCatalog'){const file=t.files?.[0];document.getElementById('catalogFileName').textContent=file?`${file.name} selected`:'No catalog selected';return;}
    if(t.id==='orderVendor')ui.orderVendor=t.value;
    else if(t.id==='orderOwner')ui.orderOwner=t.value;
    else if(t.id==='orderStatus')ui.orderStatus=t.value;
    else if(t.dataset.category){ const c=t.dataset.category; state.preauthorized=t.checked?[...new Set([...state.preauthorized,c])]:state.preauthorized.filter(x=>x!==c); save(); toast('Preauthorized categories updated and audited.'); }
    else if(t.dataset.item){ const i=t.dataset.item; state.draft.items=t.checked?[...new Set([...state.draft.items,i])]:state.draft.items.filter(x=>x!==i); if(i==='Oxygen Concentrator E1390'&&state.draft.ehr){ if(!t.checked)Object.assign(state.draft,{duplicateResolution:'remove',duplicateNote:'Requested duplicate item removed during standing-order verification.',duplicateResolvedBy:role().label,duplicateResolvedAt:new Date().toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}),duplicatePendingDisposition:'',duplicatePendingNote:''}); else if(state.draft.duplicateResolution==='remove')Object.assign(state.draft,{duplicateResolution:'',duplicateNote:'',duplicateResolvedBy:'',duplicateResolvedAt:'',duplicatePendingDisposition:'',duplicatePendingNote:''}); } state.draft.savedLocal=false; save(); }
    else if(t.dataset.vendorCategory){ const v=state.vendors.find(x=>x.id===state.selectedVendor),c=t.dataset.vendorCategory; v.categories=t.checked?[...new Set([...v.categories,c])]:v.categories.filter(x=>x!==c); save(); toast('Vendor capabilities updated with an audit entry.'); }
    else if(t.dataset.pref){ state.preferences[t.dataset.pref]=t.checked; save(); toast('Notification preference saved.'); }
    else if(t.dataset.prefValue){ state.preferences[t.dataset.prefValue]=t.value; save(); }
    else if(t.dataset.policy){ state.costPolicy[t.dataset.policy]=Math.max(0,Number(t.value)||0); save(); toast('Organization cost policy updated and audited.'); }
    else bindInputValue(t);
    render();
  });

  document.addEventListener('click',e=>{
    const t=e.target.closest('button,input[data-action]'); if(!t)return;
    const action=t.dataset.action;
    if(t.dataset.view){ state.view=t.dataset.view; state.selectedOrder=null; state.selectedVendor=null; ui.globalSearch=''; ui.profile=false; ui.notifications=false; save(); render(); return; }
    if(t.dataset.role){ state.role=t.dataset.role; save(); render(); return; }
    if(t.dataset.stage){ const next=Number(t.dataset.stage); if(next>1&&duplicateUnresolved(state.draft)){toast('Resolve the duplicate equipment flag before continuing.');ui.newStage=1;}else{ui.newStage=next;if(next===3&&!state.draft.verified&&vendorVerificationRequired(state.draft,state.draft.vendor)){ui.actionModal='vendor_verify';ui.modalId=state.draft.vendor;}} render(); return; }
    if(t.dataset.openOrder){ state.selectedVendor=null; state.selectedOrder=t.dataset.openOrder; render(); return; }
    if(t.dataset.admitPatient){ const p=(state.pendingAdmits||[]).find(x=>x.id===t.dataset.admitPatient); if(p){Object.assign(state.draft,{ehr:true,patient:p.name,address:p.location});state.pendingAdmits=state.pendingAdmits.filter(x=>x.id!==p.id);save();toast(`${p.name} loaded into draft from pending admissions.`);render();} return; }
    if(t.dataset.openVendor){ state.selectedOrder=null; state.selectedVendor=t.dataset.openVendor; ui.vendorTab='profile'; render(); return; }
    if(t.dataset.vendorTab){ ui.vendorTab=t.dataset.vendorTab; render(); return; }
    if(t.dataset.selectVendor){ state.draft.vendor=t.dataset.selectVendor; state.draft.verified=false;state.draft.vendorVerificationFailed=false;state.draft.vendorVerifyNote='';state.draft.vendorVerifiedBy='';state.draft.vendorVerifiedAt='';state.draft.vendorVerificationResponses=null; if(vendorVerificationRequired(state.draft,state.draft.vendor)){ui.actionModal='vendor_verify';ui.modalId=state.draft.vendor;} save(); render(); return; }
    if(t.dataset.approve){ ui.actionModal='approval';ui.modalId=t.dataset.approve;render();return; }
    if(t.dataset.recoverPsf){ if(!['don','executive'].includes(state.role)){toast('PSF recovery is available to DON and Executive roles.');return;}ui.actionModal='psf-recovery';ui.modalId=t.dataset.recoverPsf;render();return; }
    if(t.dataset.applyBundle){ const b=state.bundles.find(x=>x.id===t.dataset.applyBundle);if(b){state.draft.items=[...b.items];state.draft.savedLocal=false;save();toast(`${b.name} applied. Item-level checks remain active.`);render();}return; }
    if(t.dataset.notification){ const n=state.notifications.find(x=>x.id===Number(t.dataset.notification)); if(n){n.unread=false;ui.notifications=false;if(n.order&&state.orders.some(o=>o.id===n.order))state.selectedOrder=n.order;else if(n.view)state.view=n.view;save();render();}return; }
    if(t.dataset.resultType){ ui.globalSearch=''; if(t.dataset.resultType==='order'){state.selectedVendor=null;state.selectedOrder=t.dataset.resultId;}else{state.selectedOrder=null;state.selectedVendor=t.dataset.resultId;}render();return; }
    if(t.dataset.messageFilter){ui.messageFilter=t.dataset.messageFilter;render();return;}
    if(t.dataset.messageOrder){ui.messageOrder=t.dataset.messageOrder;render();return;}
    if(action==='landing'){state.screen='landing';ui.profile=false;render();}
    else if(action==='provider-login'){state.screen='provider-login';ui.profile=false;render();}
    else if(action==='vendor-login'){state.screen='vendor-login';render();}
    else if(action==='join'){state.screen='join';render();}
    else if(action==='enter-provider'){state.screen='portal';state.view='home';save();render();}
    else if(action==='vendor-demo'){signInVendor();}
    else if(action==='vendor-logout'){state.screen='landing';save();toast('Signed out of the vendor workspace.');render();}
    else if(action==='vendor-proof'){vendorSubmitProof(t.dataset.vendorOrder||t.closest('[data-vendor-order]')?.dataset.vendorOrder);}
    else if(action==='vendor-split'){vendorSplitFulfillment(t.dataset.vendorOrder||t.closest('[data-vendor-order]')?.dataset.vendorOrder);}
    else if(action==='vendor-accept-pickup'){vendorAcceptPickup(t.dataset.vendorOrder||t.closest('[data-vendor-order]')?.dataset.vendorOrder);}
    else if(action==='vendor-schedule-pickup'){vendorSchedulePickup(t.dataset.vendorOrder||t.closest('[data-vendor-order]')?.dataset.vendorOrder);}
    else if(action==='confirm-vendor-proof'){confirmVendorProof();}
    else if(action==='confirm-vendor-split'){confirmVendorSplit();}
    else if(action==='request-split-allocation'){requestSplitAllocation(t.dataset.splitOrder||t.closest('[data-split-order]')?.dataset.splitOrder);}
    else if(action==='confirm-split-allocation'){confirmSplitAllocation();}
    else if(action==='submit-enrollment'){submitEnrollment();}
    else if(action==='notifications'){ui.notifications=!ui.notifications;ui.profile=false;render();}
    else if(action==='profile'){ui.profile=!ui.profile;ui.notifications=false;render();}
    else if(action==='read-all'){state.notifications.forEach(n=>n.unread=false);save();render();}
    else if(action==='toggle-offline'){state.offline=!state.offline;save();toast(state.offline?'Offline mode: drafts save locally; submission paused.':'Secure connection restored. Draft is ready to synchronize.');render();}
    else if(action==='reset'){localStorage.removeItem('betterrx-provider-demo');state=clone(defaultState);ui.profile=false;toast('Demo restored.');render();}
    else if(action==='edit-handoff'){ui.actionModal='handoff';ui.modalId=null;render();}
    else if(action==='ack-handoff'){state.handoff.acknowledged=true;state.handoff.updated=new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'});save();toast('Shift handoff acknowledged and audited.');render();}
    else if(action==='ehr-pull'){Object.assign(state.draft,{ehr:true,patient:'Nora P.',address:'1452 East Willow Lane, Salt Lake City, UT',duplicateResolution:'',duplicateNote:'',duplicateResolvedBy:'',duplicateResolvedAt:'',duplicatePendingDisposition:'',duplicatePendingNote:''});save();toast('EHR context pulled into a draft. An existing-equipment conflict needs review.');render();}
    else if(action==='resolve-escalation'){const oid=t.dataset.escalationOrder;const o=state.orders.find(x=>x.id===oid);if(o&&o.escalations){o.escalations.forEach(e=>{if(e.to==='Clinician'&&e.status==='Pending')e.status='Resolved';});o.timeline.push('Clinician review complete · Approved · Just now');addNotification(`${o.id} clinician review complete.`,`${o.patient} · Modification approved.`,o.id);save();toast('Escalation resolved. Clinical approval recorded.');render();}}
    else if(action==='resolve-duplicate'){resolveDuplicate();}
    else if(action==='reopen-duplicate'){Object.assign(state.draft,{duplicateResolution:'',duplicateNote:'',duplicateResolvedBy:'',duplicateResolvedAt:'',duplicatePendingDisposition:'',duplicatePendingNote:''});if(!state.draft.items.includes('Oxygen Concentrator E1390'))state.draft.items.push('Oxygen Concentrator E1390');save();toast('Duplicate flag reopened for review.');render();}
    else if(action==='standing'){state.draft.standing=t.checked;state.draft.savedLocal=false;save();render();}
    else if(action==='verify-vendor'){state.draft.verified=t.checked;save();toast(t.checked?'Human vendor verification recorded.':'Vendor verification removed.');render();}
    else if(action==='open-vendor-contact'){ui.actionModal='vendor_verify';ui.modalId=state.draft.vendor;render();}
    else if(action==='copy-vendor-phone'){const v=state.vendors.find(x=>x.id===state.draft.vendor),phone=v.contacts.split(' · ').slice(-1)[0];copyText(phone);toast('Dispatch phone number copied.');}
    else if(action==='copy-vendor-email'){const v=state.vendors.find(x=>x.id===state.draft.vendor),email=v.email||({abc:'dispatch@abchomemedical.demo',mountain:'dispatch@mountaindme.demo',front:'dispatch@frontrangemedical.demo',summit:'dispatch@summitrespiratory.demo'}[v.id]||'dispatch@vendor.demo');copyText(email);toast('Dispatch email copied.');}
    else if(action==='confirm-vendor-contact'){confirmVendorContact();}
    else if(action==='save-draft'){state.draft.savedLocal=true;save();toast(state.offline?'Saved locally and encrypted. Not submitted.':'Secure draft saved.');render();}
    else if(action==='clear-draft'){state.draft=clone(defaultState.draft);ui.newStage=1;save();render();}
    else if(action==='submit-order'){submitOrder();}
    else if(action==='close-order'){state.selectedOrder=null;render();}
    else if(action==='close-vendor'){state.selectedVendor=null;render();}
    else if(action==='modify-order'){ui.actionModal='modify';ui.modalId=state.selectedOrder;render();}
    else if(action==='cancel-order'){ui.actionModal='cancel';ui.modalId=state.selectedOrder;render();}
    else if(action==='pickup-order'){ui.actionModal='pickup';ui.modalId=state.selectedOrder;render();}
    else if(action==='close-action-modal'){ui.actionModal=null;ui.modalId=null;render();}
    else if(action==='save-modification'){saveModification();}
    else if(action==='confirm-cancel'){confirmCancel();}
    else if(action==='confirm-pickup'){confirmPickup();}
    else if(action==='save-approval'){saveApproval();}
    else if(action==='save-psf-recovery'){savePsfRecovery();}
    else if(action==='save-handoff'){saveHandoff();}
    else if(action==='ack-delivery'){ackDelivery();}
    else if(action==='send-chat'){sendChat();}
    else if(action==='quality-event'){ui.vendorEvent=true;render();}
    else if(action==='new-psf'){if(!['don','executive'].includes(state.role)){toast('PSF records are available to DON and Executive roles.');return;}ui.actionModal='psf-create';ui.modalId=null;render();}
    else if(action==='save-new-psf'){saveNewPsf();}
    else if(action==='close-quality'){ui.vendorEvent=false;render();}
    else if(action==='save-quality'){saveQuality();}
  });

  function resolveDuplicate(){
    const disposition=state.draft.duplicatePendingDisposition||document.getElementById('duplicateDisposition')?.value,note=(state.draft.duplicatePendingNote||document.getElementById('duplicateNote')?.value||'').trim();
    if(!disposition){toast('Choose a duplicate disposition.');return;}
    if(!note){toast('Document what was verified before resolving the flag.');return;}
    Object.assign(state.draft,{duplicateResolution:disposition,duplicateNote:note,duplicateResolvedBy:role().label,duplicateResolvedAt:new Date().toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}),duplicatePendingDisposition:'',duplicatePendingNote:'',savedLocal:false});
    if(disposition==='remove')state.draft.items=state.draft.items.filter(i=>i!=='Oxygen Concentrator E1390');
    save();toast(disposition==='exception'&&state.role==='admission'?'Duplicate disposition saved. Case Manager approval will be required.':'Duplicate flag resolved and audited.');render();
  }
  function signInVendor(){
    const email=document.getElementById('vendorEmail')?.value.trim().toLowerCase(),password=document.getElementById('vendorPassword')?.value;
    if(email!=='dispatch@abchomemedical.demo'||password!=='prototype'){toast('Email or password is incorrect. Use the prefilled prototype credentials.');return;}
    state.screen='vendor-portal';save();toast('Signed in to ABC Home Medical.');render();
  }
  function submitEnrollment(){
    const value=id=>document.getElementById(id)?.value.trim()||'',categories=[...document.querySelectorAll('[data-enrollment-category]:checked')].map(x=>x.value),catalog=document.getElementById('enrollCatalog')?.files?.[0]?.name||'';
    const profile={company:value('enrollCompany'),contact:value('enrollContact'),phone:value('enrollPhone'),email:value('enrollEmail'),serviceArea:value('enrollServiceArea'),categories,catalog,weekday:`${value('enrollWeekdayStart')}–${value('enrollWeekdayEnd')}`,weekend:value('enrollWeekendDispatch')==='yes'?`${value('enrollWeekendStart')}–${value('enrollWeekendEnd')}`:'No weekend dispatch',holiday:value('enrollHolidayDispatch')==='none'?'No holiday dispatch':`${value('enrollHolidayDispatch')==='emergency'?'Emergency only':'Full dispatch'} · ${value('enrollHolidayStart')}–${value('enrollHolidayEnd')}`};
    if(!profile.company||!profile.contact||!profile.phone||!profile.email||!profile.serviceArea){toast('Complete all company and dispatch contact fields.');return;}
    if(profile.phone.replace(/\D/g,'').length<10){toast('Enter a valid contact phone number.');return;}
    if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(profile.email)){toast('Enter a valid dispatch email address.');return;}
    if(!categories.length&&!catalog){toast('Select at least one equipment category or upload a catalog.');return;}
    if(!value('enrollWeekdayStart')||!value('enrollWeekdayEnd')){toast('Enter Monday–Friday dispatch hours.');return;}
    if(value('enrollWeekendDispatch')==='yes'&&(!value('enrollWeekendStart')||!value('enrollWeekendEnd'))){toast('Enter weekend dispatch hours.');return;}
    if(value('enrollHolidayDispatch')!=='none'&&(!value('enrollHolidayStart')||!value('enrollHolidayEnd'))){toast('Enter holiday dispatch hours.');return;}
    state.lastEnrollment=profile;state.screen='enrollment-complete';save();toast('Network profile submitted for qualification.');render();
  }
  function confirmVendorContact(){
    const fields={equipment:document.getElementById('verifyEquipment')?.value,timeline:document.getElementById('verifyTimeline')?.value,address:document.getElementById('verifyAddress')?.value,operations:document.getElementById('verifyOperations')?.value};
    if(Object.values(fields).some(value=>!value)){toast('Answer Yes or No for all four vendor conditions.');return;}
    const note=document.getElementById('vendorVerifyNote')?.value.trim();
    if(!note){toast('Comments are required for every vendor response.');return;}
    const allYes=Object.values(fields).every(value=>value==='yes');
    Object.assign(state.draft,{verified:allYes,vendorVerificationFailed:!allYes,vendorVerifyNote:note,vendorVerifiedBy:role().label,vendorVerifiedAt:new Date().toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}),vendorVerificationResponses:fields,savedLocal:false});
    ui.actionModal=null;ui.modalId=null;save();toast(allYes?'Verified with Vendor recorded in the order audit trail.':'Vendor response recorded. This vendor remains blocked until every condition is confirmed.');render();
  }
  function submitOrder(){
    const cost=draftCost(),duplicateApproval=duplicateExceptionNeedsApproval(state.draft),level=duplicateApproval?'Case Manager':approvalLevel(cost);
    if(cost>role().limit||duplicateApproval){ const reason=duplicateApproval?`Duplicate exception: ${state.draft.duplicateNote}`:(state.draft.notes||'Cost exceeds current role authority.'); state.approvals.unshift({id:`APR-${Math.floor(Math.random()*700+300)}`,patient:state.draft.patient,order:'New draft',amount:cost,reason,level,requester:role().label,status:'Pending',duplicateDisposition:state.draft.duplicateResolution}); addNotification(`${level} approval requested.`,`${state.draft.patient} · ${duplicateApproval?'Duplicate exception':money(cost)} · Draft preserved.`,null); toast(`Draft routed to ${level} for approval.`); state.view='home'; }
    else { const id=`BRX-${10490+state.orders.length}`; const v=state.vendors.find(x=>x.id===state.draft.vendor); const duplicateEvent=state.draft.duplicateResolution?`Duplicate resolved: ${duplicateLabel(state.draft.duplicateResolution)} · ${state.draft.duplicateNote}`:'Duplicate check complete · No conflict'; const verificationEvent=state.draft.verified?`Vendor verified by ${state.draft.vendorVerifiedBy||role().label} · ${state.draft.vendorVerifyNote||'Availability confirmed'}`:'Vendor verification advisory recorded'; const timeline=['Order created · Just now','Standing orders verified · Just now',duplicateEvent,verificationEvent]; if(state.draft.duplicateResolution==='replace')timeline.push('Existing oxygen equipment exchange / return coordinated · Just now'); state.orders.unshift({id,patient:state.draft.patient,vendor:v.name,orderedBy:`Demo User · ${role().label}`,status:'Dispatched',exact:'Submitted · awaiting vendor acceptance',risk:riskForVendor(v.id),riskLabel:riskForVendor(v.id)>=50?'High':'Moderate',eta:`Within ${state.draft.timeline} hours`,items:[...state.draft.items],cost,benchmark:Math.round(cost*.86),weather:true,verified:state.draft.verified,vendorVerifyNote:state.draft.vendorVerifyNote,vendorVerifiedBy:state.draft.vendorVerifiedBy,vendorVerifiedAt:state.draft.vendorVerifiedAt,delivered:false,acknowledged:false,pickup:state.draft.duplicateResolution==='replace'?'Exchange coordination pending':'Not requested',location:'Vendor confirmation pending',messages:[],duplicateDisposition:state.draft.duplicateResolution,duplicateNote:state.draft.duplicateNote,timeline}); addNotification(`${id} submitted.`,`${v.name} has received the order.`,id); if(state.draft.duplicateResolution==='replace')addNotification(`${id} exchange coordination created.`,'Existing oxygen equipment requires return confirmation.',id); state.selectedOrder=id;toast(`${id} submitted to ${v.name}.`); }
    state.draft=clone(defaultState.draft);ui.newStage=1;save();render();
  }
  function saveModification(){
    const o=state.orders.find(x=>x.id===ui.modalId),reason=document.getElementById('modifyReason').value.trim();
    if(!reason){toast('Add the clinical reason before submitting the modification.');return;}
    const items=document.getElementById('modifyItems').value.split('\n').map(x=>x.trim()).filter(Boolean),priority=document.getElementById('modifyPriority').value;
    o.items=items;o.eta=priority.replace(' · ',' — ');o.timeline.push(`Order modified · ${reason}`);o.timeline.push(`${o.vendor} notified of post-dispatch change · Just now`);
    if(!o.escalations)o.escalations=[];
    const target=escalationTarget(state.role);
    o.escalations.push({from:role().label,to:target,reason:`Post-submission modification: ${reason}`,time:new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}),status:'Pending'});
    o.timeline.push(`Escalation: ${role().label} → ${target} · Just now`);
    addNotification(`${o.id} modification sent and escalated.`,`${o.vendor} notified. ${target} review required.`,o.id);ui.actionModal=null;ui.modalId=null;save();toast(`Order modified. Escalated to ${target} for review.`);render();
  }
  function confirmCancel(){
    const o=state.orders.find(x=>x.id===ui.modalId),note=document.getElementById('cancelNote').value.trim(),reason=document.getElementById('cancelReason').value;
    if(!note){toast('Document cancellation details before continuing.');return;}
    o.status='Complete';o.exact='Cancelled · vendor notified';o.timeline.push(`Order cancelled · ${reason}`);o.timeline.push(`Cancellation details · ${note}`);o.timeline.push(`${o.vendor} notified · Just now`);
    addNotification(`${o.id} was cancelled.`,'Vendor notification and audit entry complete.',o.id);ui.actionModal=null;ui.modalId=null;save();toast('Order cancelled and vendor notified.');render();
  }
  function confirmPickup(){
    const o=state.orders.find(x=>x.id===ui.modalId),reason=document.getElementById('pickupReason').value,windowText=document.getElementById('pickupWindow').value,note=document.getElementById('pickupNote').value.trim();
    if(o.status!=='Pending Pickup'){o.status='Pending Pickup';o.pickup='Requested';o.pickupEta=windowText;o.exact=`Pickup requested · ${windowText}`;o.timeline.push(`Pickup requested · ${reason}`);o.timeline.push(`Requested window · ${windowText}${note?` · ${note}`:''}`);addNotification(`${o.id} pickup requested.`,`${o.vendor} received the requested window.`,o.id);toast('Pickup requested and vendor notified.');}
    else if(o.pickup==='Requested'){o.pickup='Scheduled';o.pickupEta=windowText;o.exact=`Pickup scheduled · ${windowText}`;o.timeline.push(`Pickup scheduled · ${windowText}`);toast('Pickup window confirmed.');}
    else{o.pickup='Complete';o.pickupEta='Completed just now';o.status='Complete';o.exact='Picked up · return condition confirmed';o.timeline.push(`Pickup confirmed · ${reason}`);o.timeline.push(`Return condition documented${note?` · ${note}`:''}`);o.timeline.push('Order complete');toast('Pickup confirmed and order completed.');}
    ui.actionModal=null;ui.modalId=null;save();render();
  }
  function saveApproval(){
    const a=state.approvals.find(x=>x.id===ui.modalId),note=document.getElementById('approvalNote').value.trim();
    if(!note){toast('Add a decision rationale before recording the approval.');return;}
    const decision=document.getElementById('approvalDecision').value,scope=document.getElementById('approvalScope').value,expiry=document.getElementById('approvalExpiry').value;
    a.status=decision==='Approve'?'Approved':decision==='Deny'?'Denied':'Escalated';a.decision=`${scope} · ${expiry} · ${note}`;
    addNotification(`${a.order} ${a.status.toLowerCase()}.`,`${role().label} · ${scope} · ${expiry}`,a.order.startsWith('BRX')?a.order:null);ui.actionModal=null;ui.modalId=null;save();toast(`${a.status} decision recorded with scope and expiration.`);render();
  }
  function savePsfRecovery(){
    const psf=state.psfCases.find(x=>x.id===ui.modalId),amount=Number(document.getElementById('psfRecoveryAmount')?.value),outcome=document.getElementById('psfRecoveryOutcome')?.value,note=document.getElementById('psfRecoveryNote')?.value.trim();
    if(!psf)return;
    if(!Number.isFinite(amount)||amount<=0||amount>psf.revenueAtRisk){toast(`Enter recovered revenue between $1 and ${money(psf.revenueAtRisk)}.`);return;}
    if(!note){toast('Add recovery notes before closing the PSF.');return;}
    Object.assign(psf,{status:'Recovered',recovered:amount,recoveryOutcome:outcome,recoveryNote:note,recoveredBy:role().label,recoveredAt:new Date().toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})});
    addNotification(`${psf.id} recovery recorded.`,`${money(amount)} recovered · ${psf.vendor}`,psf.order);ui.actionModal=null;ui.modalId=null;save();toast(`${money(amount)} in revenue recovery recorded.`);render();
  }
  function saveNewPsf(){
    const vendor=document.getElementById('newPsfVendor')?.value,order=document.getElementById('newPsfOrder')?.value,failure=document.getElementById('newPsfFailure')?.value,status=document.getElementById('newPsfStatus')?.value,revenueAtRisk=Number(document.getElementById('newPsfExposure')?.value),note=document.getElementById('newPsfNote')?.value.trim();
    if(!Number.isFinite(revenueAtRisk)||revenueAtRisk<=0){toast('Enter the revenue at risk for this PSF.');return;}
    if(!note){toast('Document why the service failure was preventable.');return;}
    const next=240+state.psfCases.length+1,id=`PSF-${next}`;state.psfCases.unshift({id,order,vendor,failure,status,revenueAtRisk,recovered:0,opened:'Just now',note,createdBy:role().label});
    addNotification(`${id} created.`,`${vendor} · ${money(revenueAtRisk)} revenue at risk`,order);ui.actionModal=null;ui.modalId=null;save();toast(`${id} added to the recovery dashboard.`);render();
  }
  function saveHandoff(){
    const note=document.getElementById('handoffNote').value.trim();if(!note){toast('Add a handoff note before saving.');return;}
    state.handoff.note=note;state.handoff.owner=role().label;state.handoff.acknowledged=false;state.handoff.updated=new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'});ui.actionModal=null;save();toast('Clinical handoff updated for the next shift.');render();
  }
  function advancePickup(){ const o=state.orders.find(x=>x.id===state.selectedOrder); if(o.status!=='Pending Pickup'){o.status='Pending Pickup';o.exact='Pickup requested · vendor confirmation pending';o.pickup='Requested';o.pickupEta='Vendor confirmation pending';o.timeline.push('Pickup requested · reason documented');toast('Pickup requested and vendor notified.');}else if(o.pickup==='Requested'){o.pickup='Scheduled';o.pickupEta='Tomorrow · 10 AM–12 PM';o.exact='Pickup scheduled · Tomorrow 10 AM–12 PM';o.timeline.push('Pickup scheduled · Tomorrow 10 AM–12 PM');toast('Pickup window confirmed.');}else{o.pickup='Complete';o.pickupEta='Completed just now';o.status='Complete';o.exact='Picked up · return condition confirmed';o.timeline.push('Pickup confirmed · return condition documented');o.timeline.push('Order complete');toast('Pickup confirmed and order completed.');}save();render(); }
  function ackDelivery(){ const o=state.orders.find(x=>x.id===state.selectedOrder);o.acknowledged=true;o.archivedMessages=true;o.timeline.push('Delivery acknowledged · Just now');o.timeline.push('Message thread closed and auto-archived · Just now');addNotification(`${o.id} delivery acknowledged.`,'Conversation auto-archived. Executive access only.',o.id);save();toast('Delivery acknowledged. Messages auto-archived for Executive access.');render(); }
  function sendChat(){ const input=document.getElementById('chatInput');if(!input||!input.value.trim())return; const id=state.selectedOrder||ui.messageOrder;const o=state.orders.find(x=>x.id===id);o.messages.push({me:true,text:input.value.trim(),time:new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})});save();toast('In-app message sent and attached to the order.');render(); }
  function saveQuality(){ const v=state.vendors.find(x=>x.id===state.selectedVendor),note=document.getElementById('qeNote').value.trim();if(!note){toast('Add event details before saving.');return;}v.events.unshift({type:document.getElementById('qeType').value,severity:document.getElementById('qeSeverity').value,category:document.getElementById('qeCategory').value,note,date:'Today'});ui.vendorEvent=false;save();toast('Quality event saved. Rules-based ranking impact applied in the background.');render(); }

  function clinicianView(){
    const escalated=state.orders.filter(o=>(o.escalations||[]).some(e=>e.to==='Clinician'&&e.status==='Pending'));
    const withMessages=state.orders.filter(o=>o.messages.length&&!o.archivedMessages);
    const current=withMessages.find(o=>o.id===ui.messageOrder)||withMessages[0];
    return `${titleBlock('Clinical Review','Clinician Workspace','Escalated orders requiring clinical decision and active delivery messaging.')}
      <div class="grid two"><section class="card"><div class="card-head"><div><h2>Escalated Orders</h2><p class="muted">Post-submission modifications requiring clinical review</p></div>${pill(`${escalated.length} pending`,'red')}</div>
      ${escalated.length?`<div class="list">${escalated.map(o=>{const pending=(o.escalations||[]).filter(e=>e.to==='Clinician'&&e.status==='Pending');return `<div class="row high"><div class="row-main"><strong>${esc(o.patient)} · ${esc(o.id)}</strong><small>${esc(o.vendor)} · ${esc(o.items.join(' · '))}</small>${pending.map(e=>`<small><strong>Escalated by:</strong> ${esc(e.from)} · ${esc(e.reason)}</small><small><strong>Time:</strong> ${esc(e.time)}</small>`).join('')}</div><div class="row-meta">${pill('Clinical Review','red')}<button class="btn small" style="margin-top:7px" data-action="resolve-escalation" data-escalation-order="${o.id}">Resolve</button></div></div>`;}).join('')}</div>`:`<div class="empty">No orders currently escalated to Clinician.</div>`}</section>
      <section class="card"><div class="card-head"><div><h2>Active Messages</h2><p class="muted">Delivery conversations for clinical awareness</p></div></div>
      ${withMessages.length?`<div class="list" style="margin-bottom:14px">${withMessages.map(o=>`<button class="row" data-message-order="${o.id}" style="width:100%;text-align:left"><div class="row-main"><strong>${esc(o.patient)} · ${esc(o.id)}</strong><small>${esc(o.vendor)} · ${o.status}</small></div>${pill('Active','green')}</button>`).join('')}</div>${current?`<div class="card-head"><div><h2>${esc(current.patient)}</h2><p class="muted">${current.id} · ${current.vendor}</p></div>${pill('Active delivery','green')}</div>${chatMarkup(current)}`:''}`:`<div class="empty">No active delivery conversations.</div>`}</section></div>`;
  }

  function escalateModification(orderId){
    const o=state.orders.find(x=>x.id===orderId);if(!o)return;
    if(!o.escalations)o.escalations=[];
    const target=escalationTarget(state.role);
    o.escalations.push({from:role().label,to:target,reason:'Post-submission order modification requires review',time:new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}),status:'Pending'});
    o.timeline.push(`Escalation: ${role().label} → ${target} · Just now`);
    addNotification(`${o.id} escalated to ${target}.`,`${o.patient} · Post-submission change requires review.`,o.id);
    save();toast(`Order escalated to ${target} for review.`);render();
  }


  function vendorSubmitProof(orderId){if(!orderId)return;ui.actionModal='vendor-proof';ui.modalId=orderId;render();}
  function confirmVendorProof(){const o=state.orders.find(x=>x.id===ui.modalId);if(!o)return;o.vendorProofSubmitted=true;o.timeline.push('Proof of delivery submitted · '+new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}));ui.actionModal=null;ui.modalId=null;save();toast('Proof of delivery submitted successfully.');render();}
  function vendorSplitFulfillment(orderId){if(!orderId)return;ui.actionModal='vendor-split';ui.modalId=orderId;render();}
  function confirmVendorSplit(){const o=state.orders.find(x=>x.id===ui.modalId);if(!o)return;const reason=(document.getElementById('splitReason')?.value||'').trim();if(!reason){toast('Provide a reason for the split fulfillment.');return;}o.vendorSplit=true;o.splitReason=reason;o.timeline.push('Split Fulfillment initiated · '+reason);o.timeline.push('DON and Executive notified of split delivery · '+new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}));addNotification(o.id+' split fulfillment.',o.patient+' · Vendor initiated split: '+reason,o.id);ui.actionModal=null;ui.modalId=null;save();toast('Order marked as Split Delivery. Notifications sent.');render();}
  function vendorAcceptPickup(orderId){const o=state.orders.find(x=>x.id===orderId);if(!o)return;o.pickup='Accepted';o.timeline.push('Vendor accepted pickup request · '+new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}));save();toast('Pickup accepted.');render();}
  function vendorSchedulePickup(orderId){const o=state.orders.find(x=>x.id===orderId);if(!o)return;o.pickup='Scheduled';o.pickupEta='Tomorrow · 10 AM–12 PM';o.timeline.push('Vendor scheduled pickup · Tomorrow 10 AM–12 PM');save();toast('Pickup scheduled for tomorrow.');render();}
  function requestSplitAllocation(orderId){if(!orderId)return;ui.actionModal='split-allocation';ui.modalId=orderId;render();}
  function confirmSplitAllocation(){const o=state.orders.find(x=>x.id===ui.modalId);if(!o)return;const reason=(document.getElementById('splitAllocReason')?.value||'').trim();if(!reason){toast('Provide clinical justification for split allocation.');return;}const allocations=[];document.querySelectorAll('[data-alloc-vendor]').forEach(sel=>{allocations.push({item:o.items[Number(sel.dataset.allocVendor)],vendor:sel.value});});o.splitAllocation={reason,allocations,status:'Pending DON Approval',requestedBy:role().label,requestedAt:new Date().toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})};o.timeline.push('Split allocation requested · Routed to DON for approval');addNotification(o.id+' split allocation requested.',o.patient+' · Case Manager requests multi-vendor split. DON approval required.',o.id);ui.actionModal=null;ui.modalId=null;save();toast('Split allocation request submitted to DON.');render();}

  render();
})();
