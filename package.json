{
  "name": "betterrx-dme-direct-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "BetterRX + DME+ DME Direct provider and vendor workflow prototype",
  "scripts": {
    "dev": "npx --yes http-server . -p 4173 -c-1",
    "validate": "node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
